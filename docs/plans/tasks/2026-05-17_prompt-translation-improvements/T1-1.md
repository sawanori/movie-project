# T1-1: TranslateStoryPromptInput / ExtractedComponents dataclass 追加

## 依存タスク

なし (他全タスクの前提)

## 目的

`gemini_client.py` に 2 つの dataclass を追加し、後続タスクが依存できる型基盤を確立する。`translate_story_prompt` の引数を object に集約することで 3+ パラメータ問題を解消し、`ExtractedComponents` の戻り値 contract を型で保証する (B3 対応)。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/external/gemini_client.py`
  - ファイル先頭の import ブロック直後 (`from app.core.config import settings` の下) に dataclass 定義を追加

## 実装手順

1. `gemini_client.py` の冒頭 import を確認し、`dataclasses` / `typing` が import 済か確認する
2. 不足している場合 `from dataclasses import dataclass, field` と `from typing import Optional, Literal` を追加する
3. 以下の dataclass 2 つをモジュールトップレベル (ログ設定直後、既存関数より前) に追加する:

```python
from dataclasses import dataclass
from typing import Optional, Literal

SubjectType = Literal["person", "object", "animation"]
AnimationCategory = Literal["2d", "3d"]


@dataclass
class TranslateStoryPromptInput:
    """translate_story_prompt の引数集約 dataclass。

    引数増加に伴うシグネチャ複雑化を防ぐ。フロント (TypeScript)
    からの JSON request body をそのまま展開して構築できる構造。
    """
    description_ja: str
    video_provider: str = "runway"
    subject_type: SubjectType = "person"
    camera_work: Optional[str] = None
    animation_category: Optional[AnimationCategory] = None
    animation_template: Optional[str] = None
    # Act-Two 関連 (Phase 1 では未使用、Phase 2 で利用)
    use_act_two: bool = False
    motion_type: Optional[str] = None
    expression_intensity: int = 3
    body_control: bool = True


@dataclass
class ExtractedComponents:
    """_extract_prompt_components の戻り値 contract (B3 対応)。

    型を統一して TypeError / AttributeError リスクを排除:
    - 文字列フィールドは常に str (空なら "")。None は許容しない。
    - dialogue のみ Optional[str]。null は「セリフなし」を表現。
    """
    subject_visual: str   # 必ず str (空なら "")
    action: str           # 必ず str (空なら "")
    camera: str           # 必ず str (空なら "")
    micro_expression: str  # 必ず str (空なら "")
    lighting: str         # 必ず str (空なら "")
    other: str            # 必ず str (空なら "")
    must_include: str     # 必ず str (空なら "")
    dialogue: Optional[str] = None  # 唯一 None 許容
```

4. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate  # または: python3 -m venv venv && source venv/bin/activate
python -c "from app.external.gemini_client import TranslateStoryPromptInput, ExtractedComponents; print('OK')"
```

エラーなく `OK` が表示されることを確認する。

```bash
# 型チェック (mypy がある場合)
python -c "
from app.external.gemini_client import TranslateStoryPromptInput, ExtractedComponents
p = TranslateStoryPromptInput(description_ja='テスト')
assert p.subject_type == 'person'
assert p.use_act_two == False
e = ExtractedComponents(subject_visual='test', action='', camera='', micro_expression='', lighting='', other='', must_include='')
assert e.dialogue is None
print('dataclass instantiation OK')
"
```

## 完了条件

- `TranslateStoryPromptInput` と `ExtractedComponents` がモジュールから import できる
- `TranslateStoryPromptInput(description_ja="test")` でデフォルト値込みでインスタンス化できる
- `ExtractedComponents` の `dialogue` フィールドだけが `Optional[str]`、他は `str`
- 既存テストが壊れていない: `pytest tests/ -v --ignore=tests/videos/test_text_to_image.py --ignore=tests/library/ -q` が追加エラーなし

## コミットメッセージ案

```
feat(translate): add TranslateStoryPromptInput and ExtractedComponents dataclasses

Establishes type contract for the new translate_story_prompt function.
ExtractedComponents enforces str-only fields (except dialogue: Optional[str])
to prevent TypeError/AttributeError in downstream callers.
```
