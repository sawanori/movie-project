# T2-3: PromptNode に確認カード UI 追加

## 依存タスク

T2-1 完了後 (T2-2/T2-4 とは並列着手可、ただし T2-2 と同一ファイル編集のため T2-2 のマージ後推奨)

## 目的

セリフ検出時に PromptNode 内に確認カード UI を表示する。`pendingDialogue` state と `isTranslating` 状態の組み合わせで表示制御し、「新規 DialogueNode を作成」「既存ノードに転記」「無視」の 3 ボタンを提供する (案 A 確認カード方式、AC-C4 対応)。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/PromptNode.tsx`
  - `pendingDialogue` state 追加
  - `useEffect` に `extracted_dialogue` ハンドリング追加 (T2-2 の変更の上に積む)
  - JSX に確認カード UI ブロック追加

## 実装手順

前提: T2-2 の変更がマージ済みであること。

1. `PromptNode.tsx` の import に `MessageSquare` を追加する (lucide-react):

```typescript
import { Type, Loader2, Languages, MessageSquare } from 'lucide-react';
```

2. コンポーネント内に `pendingDialogue` state を追加する (T2-4 の `dismissedDialogueHashRef` とは別タスクなので、この時点では dismiss 機能なし):

```typescript
// セリフ検出状態 (確認カード表示用)
const [pendingDialogue, setPendingDialogue] = useState<string | null>(null);
```

3. T2-2 で変更した `useEffect` の `updateNodeData` 呼び出し後に `extracted_dialogue` ハンドリングを追加する:

```typescript
// updateNodeData の後に追加
updateNodeData({
  englishPrompt: result.english_prompt,
  isTranslating: false,
  isValid: true,
  errorMessage: undefined,
});

// セリフ検出時の表示制御
// NOTE: dismiss 抑制ロジック (dismissedDialogueHashRef) は T2-4 で追加する
if (result.extracted_dialogue) {
  setPendingDialogue(result.extracted_dialogue);
} else {
  setPendingDialogue(null);
}
```

4. JSX の英訳プロンプト表示直後に確認カード UI ブロックを追加する (翻訳結果表示の `<div>` 直後):

```tsx
{pendingDialogue && !data.isTranslating && (
  <div className="mt-2 p-2 bg-amber-900/30 border border-amber-700/50 rounded-lg">
    <div className="flex items-center gap-1 text-xs text-amber-300 mb-1">
      <MessageSquare className="w-3 h-3" />
      <span>セリフを検出しました</span>
    </div>
    <p className="text-xs text-gray-200 mb-2 italic">「{pendingDialogue}」</p>
    <div className="flex gap-1 flex-wrap">
      <button
        onClick={() => handleCreateDialogueNode(pendingDialogue)}
        className="px-2 py-1 text-xs bg-blue-600 hover:bg-blue-500 text-white rounded"
      >
        新規 DialogueNode を作成
      </button>
      <button
        onClick={() => handleSendToExistingDialogue(pendingDialogue)}
        disabled={!hasExistingDialogueNode}
        className="px-2 py-1 text-xs bg-gray-700 hover:bg-gray-600 disabled:opacity-40 text-white rounded"
      >
        既存ノードに転記
      </button>
      <button
        onClick={() => {
          // dismiss: T2-4 で dismissedDialogueHashRef を追加するまでは単純非表示
          setPendingDialogue(null);
        }}
        className="px-2 py-1 text-xs bg-transparent hover:bg-gray-700 text-gray-400 rounded"
      >
        無視
      </button>
    </div>
  </div>
)}
```

5. ハンドラ関数のスタブを追加する (T2-5 で本実装に差し替え):

```typescript
// スタブ: T2-5 で CustomEvent 発火実装に置き換え
const handleCreateDialogueNode = useCallback((_dialogueText: string) => {
  // TODO: T2-5 で実装
  setPendingDialogue(null);
}, []);

const handleSendToExistingDialogue = useCallback((_dialogueText: string) => {
  // TODO: T2-5 で実装
  setPendingDialogue(null);
}, []);

// 既存 DialogueNode の存在チェック (スタブ: 常に false)
// T2-5 で実際の nodes 参照に置き換え
const hasExistingDialogueNode = false;
```

6. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1 | head -30
npm run lint 2>&1 | head -30
```

型エラーなし、lint エラーなし。

開発サーバーで動作確認:

```bash
npm run dev
```

- ブラウザで `/generate` を開き、NodeEditor に PromptNode を配置
- 日本語プロンプトに「`「こんにちは」`」を含む文を入力
- 翻訳完了後に「セリフを検出しました」カードが表示されること (API が実際に呼ばれる)

## 完了条件

- `pendingDialogue` state が追加されている
- `extracted_dialogue` が non-null の場合に確認カードが表示される (AC-C4)
- 確認カードに 3 ボタン (新規作成・転記・無視) が存在する
- `isTranslating === true` の間はカードが表示されない
- 「無視」ボタンクリックでカードが非表示になる
- `npx tsc --noEmit` エラーなし

## コミットメッセージ案

```
feat(PromptNode): add dialogue detection confirmation card UI

Shows amber card with 3 action buttons when extracted_dialogue is detected
(AC-C4). Handlers are stubs — T2-5 will implement CustomEvent dispatch.
setPendingDialogue controls visibility without dismiss hash (added in T2-4).
```
