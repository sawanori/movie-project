# T3-2: 回帰確認 (既存 translate_scene_to_runway_prompt が壊れていないこと)

## 依存タスク

Phase 1 (T1-1〜T1-8) + Phase 2 (T2-1〜T2-6) 全完了後

## 目的

既存の `translate_scene_to_runway_prompt` (storyboard 経由の翻訳、`POST /storyboard/translate-scene`) が本 doc の変更で壊れていないことを確認する。BE の新規関数追加は別関数として実装しているため通常は影響しないが、import の汚染・型競合・ルーターの意図しない上書きがないことを検証する。

## 対象ファイル

- 参照のみ (変更なし):
  - `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/external/gemini_client.py` (既存 `translate_scene_to_runway_prompt`)
  - `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/videos/router.py` (既存 `/storyboard/translate-scene`)
- 確認: テスト実行結果

## 実装手順

このタスクは実装ではなく検証。以下のコマンドを順に実行する。

### ステップ 1: BE テスト全体の実行

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate

# 全テスト実行 (既存失敗中の 3 件は --ignore で除外)
pytest tests/ -v \
  --ignore=tests/videos/test_text_to_image.py \
  --ignore=tests/library/test_service.py \
  2>&1 | tail -40
```

期待: `tests/external/test_gemini_translate_story.py` の新規 10 件 PASSED、他の既存テストに新規失敗なし。

### ステップ 2: 既存エンドポイントが存在することを確認

```bash
python -c "
from app.videos.router import router
routes = [r.path for r in router.routes]
assert '/storyboard/translate-scene' in routes, f'Missing route: {routes}'
assert '/story/translate' in routes, f'Missing route: {routes}'
print('Both routes exist:', [r for r in routes if 'translate' in r])
"
```

### ステップ 3: 既存関数が import できることを確認

```bash
python -c "
from app.external.gemini_client import translate_scene_to_runway_prompt
import inspect
assert inspect.iscoroutinefunction(translate_scene_to_runway_prompt)
print('translate_scene_to_runway_prompt import OK')
"
```

### ステップ 4: FE の回帰確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker

# 型チェック
npx tsc --noEmit 2>&1 | head -20

# lint
npm run lint 2>&1 | head -20

# テスト全体
npm run test 2>&1 | tail -20
```

### ステップ 5: 開発サーバーでの実機確認 (L1 検証)

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
uvicorn app.main:app --port 8000 &

# 新エンドポイント: extracted_dialogue が返ること
curl -s -X POST http://localhost:8000/api/v1/videos/story/translate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TEST_TOKEN>" \
  -d '{"description_ja": "ベージュのセーターが「こんにちは」と言う", "subject_type": "object"}' \
  | python -m json.tool
# 期待: {"english_prompt": "...", "extracted_dialogue": "こんにちは"}

# 既存エンドポイント: 変更されていないこと
curl -s -X POST http://localhost:8000/api/v1/videos/storyboard/translate-scene \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TEST_TOKEN>" \
  -d '{"description_ja": "女性が振り向く", "scene_number": 1}' \
  | python -m json.tool
# 期待: {"runway_prompt": "..."} (extracted_dialogue なし)

kill %1 2>/dev/null || true
```

## 完了条件

- BE テスト全体で新規失敗なし (既存の 3 件失敗は除外済み)
- `translate_scene_to_runway_prompt` が正常に import できる
- `/storyboard/translate-scene` エンドポイントが存在する
- `/story/translate` エンドポイントが `extracted_dialogue` を返す
- FE の `npx tsc --noEmit` / `npm run lint` / `npm run test` が全パス
- 実機 curl で既存エンドポイント (`/storyboard/translate-scene`) が正常レスポンス (optional、環境依存)

## コミットメッセージ案

このタスクは実装がないためコミット不要。検証結果をレポートするだけでよい。

もし軽微な修正が必要になった場合:

```
fix(translate): resolve regression in translate_scene_to_runway_prompt import

[修正内容を記述]
```
