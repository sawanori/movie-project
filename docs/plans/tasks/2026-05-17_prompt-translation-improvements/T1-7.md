# T1-7: /api/v1/videos/story/translate ルーター更新

## 依存タスク

T1-5 + T1-6 が両方完了後

## 目的

`router.py` の `POST /api/v1/videos/story/translate` エンドポイントを、新しい `translate_story_prompt` 関数 (dataclass 引数) に切り替える。B2 対応: フロントから渡された `subject_type` 等を確実に `TranslateStoryPromptInput` に流し込む。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/videos/router.py`
  - 既存の `POST /api/v1/videos/story/translate` ハンドラ関数を新実装に置き換える

## 実装手順

1. `router.py` で `story/translate` エンドポイントを grep して現在の実装位置を確認する:

```bash
grep -n "story/translate\|translate_story_prompt" /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/videos/router.py
```

2. 現在のハンドラを確認し、以下の新実装に置き換える。注意: 既存の `translate_scene_description` (`POST /storyboard/translate-scene`) は変更しない。

新ハンドラ:

```python
@router.post("/story/translate", response_model=TranslateStoryPromptResponse)
async def translate_story_prompt_endpoint(
    request: TranslateStoryPromptRequest,
    current_user: dict = Depends(get_current_user),
):
    """PromptNode 用: 日本語プロンプトを英語に翻訳し、セリフを分離抽出する。

    B2 対応: subject_type / camera_work 等の全オプションを
    TranslateStoryPromptInput に流し込む。
    """
    from app.external.gemini_client import (
        translate_story_prompt,
        TranslateStoryPromptInput,
    )

    try:
        # B2 対応: フロント側で渡された subject_type 等を確実に流し込む
        params = TranslateStoryPromptInput(
            description_ja=request.description_ja,
            video_provider=request.video_provider.value,
            subject_type=request.subject_type.value,
            camera_work=request.camera_work,
            animation_category=(
                request.animation_category.value
                if request.animation_category else None
            ),
            animation_template=request.animation_template,
            use_act_two=request.use_act_two,
            motion_type=request.motion_type,
            expression_intensity=request.expression_intensity,
            body_control=request.body_control,
        )
        english_prompt, extracted_dialogue = await translate_story_prompt(params)
        return TranslateStoryPromptResponse(
            english_prompt=english_prompt,
            extracted_dialogue=extracted_dialogue,
        )
    except Exception as e:
        logger.exception(f"Story prompt translation failed: {e}")
        raise HTTPException(
            status_code=500,
            detail=f"翻訳に失敗しました: {str(e)}",
        )
```

注意点:
- `animation_template` は `schemas.py` で `Optional[str]` として定義されており、`.value` 変換は不要 (直接 `request.animation_template` を渡す)
- `TranslateStoryPromptRequest` は既存 schema (`schemas.py:601-613`) をそのまま使用する
- `TranslateStoryPromptResponse` は T1-6 で `extracted_dialogue` を追加済みのもの

3. 既存エンドポイント `POST /storyboard/translate-scene` (L1391-1451) が変更されていないことを確認する

4. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate

# サーバー起動テスト (インポートエラーなし)
python -c "
from app.videos.router import router
print('router import OK')
"

# 開発サーバーを起動して curl でテスト
uvicorn app.main:app --port 8001 &
sleep 3

curl -s -X POST http://localhost:8001/api/v1/videos/story/translate \
  -H "Content-Type: application/json" \
  -d '{
    "description_ja": "ベージュのニットセーター。「こんにちは」と言う",
    "subject_type": "object",
    "video_provider": "runway"
  }' | python -m json.tool

# 期待: {"english_prompt": "...", "extracted_dialogue": "こんにちは"}

# プロセス停止
kill %1 2>/dev/null || true
```

既存エンドポイントの回帰確認:

```bash
# /storyboard/translate-scene が壊れていないこと
python -c "
import ast, sys
with open('/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/videos/router.py') as f:
    src = f.read()
assert 'storyboard/translate-scene' in src
assert 'translate_scene_to_runway_prompt' in src
print('existing translate-scene endpoint preserved OK')
"
```

## 完了条件

- `POST /api/v1/videos/story/translate` が `translate_story_prompt(params)` を呼び出す
- レスポンスに `english_prompt` と `extracted_dialogue` が両方含まれる
- `subject_type` 等の全オプションが `TranslateStoryPromptInput` に渡されている
- 既存 `POST /storyboard/translate-scene` ハンドラが変更されていない
- `pytest tests/ -v -q` で既存テストに新規エラーなし

## コミットメッセージ案

```
feat(router): update /story/translate to call translate_story_prompt with dataclass

Passes all optional fields (subject_type, camera_work, animation_*, use_act_two)
from request to TranslateStoryPromptInput (B2 fix). Returns extracted_dialogue
in response. Existing /storyboard/translate-scene is untouched.
```
