# T2-2: PromptNode 翻訳呼び出しに subject_type 等を含める

## 依存タスク

T2-1 完了後 (T2-3/T2-4 とは並列着手可)

## 目的

PromptNode の `useEffect` 内の翻訳呼び出しを、`subject_type` / `camera_work` / `animation_*` / `use_act_two` 等の全オプションを含む形に更新する (B2 解消)。現状は `{ description_ja: localPrompt }` だけ渡しており、`subject_type` が BE に届いていない。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/PromptNode.tsx`
  - L41-73 の `useEffect` (デバウンス翻訳) を変更

## 実装手順

1. `PromptNode.tsx` の L41-73 を確認する (現在の `useEffect` 定義):

```typescript
// デバウンス翻訳
useEffect(() => {
  if (!localPrompt.trim()) {
    updateNodeData({
      japanesePrompt: '',
      englishPrompt: '',
      isValid: false,
    });
    return;
  }

  const timer = setTimeout(async () => {
    updateNodeData({ isTranslating: true, japanesePrompt: localPrompt });

    try {
      const result = await videosApi.translateStoryPrompt({ description_ja: localPrompt });
      updateNodeData({
        englishPrompt: result.english_prompt,
        isTranslating: false,
        isValid: true,
        errorMessage: undefined,
      });
    } catch (error) {
      updateNodeData({
        isTranslating: false,
        isValid: false,
        errorMessage:
          error instanceof Error ? error.message : '翻訳に失敗しました',
      });
    }
  }, 500);

  return () => clearTimeout(timer);
}, [localPrompt, updateNodeData]);
```

2. `useEffect` を以下のように変更する (T2-3/T2-4 が追加する `pendingDialogue` 管理は含めない — 別タスク):

```typescript
// デバウンス翻訳 (B2 対応: 全翻訳パラメータを渡す)
useEffect(() => {
  if (!localPrompt.trim()) {
    updateNodeData({
      japanesePrompt: '',
      englishPrompt: '',
      isValid: false,
    });
    return;
  }

  const timer = setTimeout(async () => {
    updateNodeData({ isTranslating: true, japanesePrompt: localPrompt });

    try {
      // B2 対応: subject_type を含む全オプションを翻訳 API に渡す
      const result = await videosApi.translateStoryPrompt({
        description_ja: localPrompt,
        subject_type: (data.subjectType ?? 'person') as 'person' | 'object' | 'animation',
        camera_work: data.cameraWork,
        animation_category: data.animationCategory as '2d' | '3d' | null | undefined,
        animation_template: data.animationTemplate,
        use_act_two: data.useActTwo ?? false,
        motion_type: data.motionType,
        expression_intensity: data.expressionIntensity ?? 3,
        body_control: data.bodyControl ?? true,
      });

      updateNodeData({
        englishPrompt: result.english_prompt,
        isTranslating: false,
        isValid: true,
        errorMessage: undefined,
      });
      // NOTE: result.extracted_dialogue の処理は T2-3 で追加する
    } catch (error) {
      updateNodeData({
        isTranslating: false,
        isValid: false,
        errorMessage:
          error instanceof Error ? error.message : '翻訳に失敗しました',
      });
    }
  }, 500);

  return () => clearTimeout(timer);
  // B2 対応: 翻訳パラメータ変更時も再翻訳トリガ
}, [
  localPrompt,
  updateNodeData,
  data.subjectType,
  data.cameraWork,
  data.animationCategory,
  data.animationTemplate,
  data.useActTwo,
  data.motionType,
  data.expressionIntensity,
  data.bodyControl,
]);
```

注意: `data.cameraWork` / `data.useActTwo` / `data.motionType` 等のフィールドが `PromptNodeData` 型に存在するか確認する。存在しない場合は `data.cameraWork as string | undefined` のような安全な型アクセスを使う。

3. `PromptNodeData` 型に不足フィールドがある場合の対処:

```bash
grep -n "PromptNodeData\|cameraWork\|useActTwo\|motionType" \
  /Users/noritakasawada/AI_P/practice/movie-project/movie-maker/lib/types/node-editor.ts
```

型定義に不足フィールドがあれば `node-editor.ts` に追加する (省略可能フィールドとして)。

4. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1 | head -30
```

型エラーなし。

```bash
npm run lint 2>&1 | head -30
```

lint エラーなし。

## 完了条件

- `videosApi.translateStoryPrompt` 呼び出しに `subject_type` / `camera_work` 等が含まれる
- `useEffect` の依存配列に `data.subjectType` 等が含まれる (subjectType 変更時に再翻訳される)
- `npx tsc --noEmit` でエラーなし
- `npm run lint` でエラーなし

## コミットメッセージ案

```
feat(PromptNode): pass subject_type and all options to translateStoryPrompt API

B2 fix: previously only description_ja was sent, causing subject_type to default
to 'person' on the backend regardless of user selection.
useEffect deps updated to retranslate when any param changes.
```
