# T1-2: _run_gemini_translation 分離関数追加 + asyncio.to_thread 化

## 依存タスク

T1-1 完了後 (T1-3/T1-4 とは並列着手可)

## 目的

翻訳実行ロジックを `_run_gemini_translation(system_prompt, user_input) -> str` として独立関数化し、`asyncio.to_thread` で Gemini SDK (同期 API) を event loop ブロックなしで呼び出す。N5/N6 対応: この関数を分離することでテスト時の monkeypatch 対象を 1 箇所に絞る。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/external/gemini_client.py`
  - `TranslateStoryPromptInput` / `ExtractedComponents` の定義直後に追加

## 実装手順

1. `gemini_client.py` のトップ import に `import asyncio` が含まれているか確認する (既存コードで `translate_scene_to_runway_prompt` 等が使っている可能性がある)
2. 不足なら `import asyncio` を追加する (`google.genai` import ブロックの近くに)
3. 以下の関数をモジュールに追加する (T1-1 で定義した dataclass の後):

```python
async def _run_gemini_translation(system_prompt: str, user_input: str) -> str:
    """N5 / N6 対応: 翻訳実行を分離関数化 (テストで monkeypatch しやすい)。

    Gemini SDK の generate_content は同期 API のため asyncio.to_thread で
    event loop のブロックを回避する。

    Args:
        system_prompt: システム指示プロンプト (テンプレート込み)
        user_input: ユーザー入力の日本語テキスト

    Returns:
        Gemini が生成した英語プロンプト文字列 (strip 済)

    Raises:
        Exception: Gemini API 呼び出し失敗時はそのまま上位に伝播
    """
    client = get_gemini_client()
    response = await asyncio.to_thread(
        client.models.generate_content,
        model="gemini-2.0-flash",
        contents=user_input,
        config=types.GenerateContentConfig(
            system_instruction=system_prompt,
            temperature=0.6,
        ),
    )
    return response.text.strip()
```

注意: `get_gemini_client()` は既存関数。`types` は `from google.genai import types` で既にインポートされている。

4. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate
python -c "
from app.external.gemini_client import _run_gemini_translation
import inspect
assert inspect.iscoroutinefunction(_run_gemini_translation), 'must be async'
print('_run_gemini_translation is async OK')
"
```

monkeypatch の動作確認 (pytest の asyncio モードで):

```python
# 仮テストコード (ファイルには書かない、手動確認用)
import pytest
from unittest.mock import AsyncMock, patch

@pytest.mark.asyncio
async def test_run_gemini_translation_mockable():
    with patch(
        "app.external.gemini_client._run_gemini_translation",
        new=AsyncMock(return_value="Preserve the design.")
    ) as m:
        result = await _run_gemini_translation("sys", "input")
        # このテストは mock なので Gemini を呼ばない
        assert result == "Preserve the design."
```

## 完了条件

- `_run_gemini_translation` が `async def` で定義されている
- `asyncio.to_thread` 経由で Gemini SDK を呼び出している
- `return response.text.strip()` で文字列を返す
- 既存テストが壊れていない

## コミットメッセージ案

```
feat(translate): add _run_gemini_translation async helper with asyncio.to_thread

Isolates Gemini API call into a single mockable function (N5/N6).
Uses asyncio.to_thread to prevent blocking the event loop with
the synchronous Gemini SDK.
```
