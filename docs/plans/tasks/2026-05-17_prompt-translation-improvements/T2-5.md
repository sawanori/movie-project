# T2-5: createDialogueNodeFromPrompt CustomEvent + NodeEditor listener 追加

## 依存タスク

T2-3 完了後 (T2-4 完了後を推奨)

## 目的

PromptNode の「新規 DialogueNode を作成」ボタンで `createDialogueNodeFromPrompt` CustomEvent を発火し、NodeEditor 側の listener で新規 DialogueNode を `setNodes` の関数形式 setter で追加する (N2/N4 対応)。「既存ノードに転記」は既存の `nodeDataUpdate` CustomEvent パターンを再利用する。T2-3 で追加したスタブハンドラを本実装に置き換える。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/PromptNode.tsx`
  - `handleCreateDialogueNode` / `handleSendToExistingDialogue` スタブを本実装に差し替え
  - `hasExistingDialogueNode` の実装 (NodeEditor から nodes 参照)
- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/NodeEditor.tsx`
  - `createDialogueNodeFromPrompt` listener を追加

## 実装手順

### PromptNode.tsx の変更

1. `PromptNode.tsx` で NodeEditor の nodes を取得する方法を確認する:

```bash
grep -n "useReactFlow\|useStore\|useNodes\|getNodes" \
  /Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/PromptNode.tsx
```

PromptNode は通常 ReactFlow のコンテキスト内にあるため `useReactFlow` を使える。

2. `@xyflow/react` から `useReactFlow` を import する (既存 import を確認):

```typescript
import { Handle, Position, NodeProps, useReactFlow } from '@xyflow/react';
```

3. コンポーネント内に `getNodes` を取得する:

```typescript
const { getNodes } = useReactFlow();
```

4. `hasExistingDialogueNode` を実際の nodes 参照に変更する:

```typescript
// 既存 DialogueNode の存在チェック (T2-3 のスタブを本実装に差し替え)
const hasExistingDialogueNode = getNodes().some(n => n.type === 'dialogue');
```

注意: `hasExistingDialogueNode` は render 時に評価される。ボタンの `disabled` 状態は render ごとに更新される。

5. `handleCreateDialogueNode` を本実装に差し替える:

```typescript
const handleCreateDialogueNode = useCallback((dialogueText: string) => {
  // N2 / N4 対応: createDialogueNodeFromPrompt CustomEvent を NodeEditor に伝達
  const event = new CustomEvent('createDialogueNodeFromPrompt', {
    detail: {
      sourcePromptNodeId: id,
      initialText: dialogueText,
    },
  });
  window.dispatchEvent(event);

  // dismiss 記録
  dismissedDialogueHashRef.current = normalizeDialogue(dialogueText);
  setPendingDialogue(null);
}, [id, normalizeDialogue]);
```

注意: T2-4 で `dismissedDialogueHashRef` と `normalizeDialogue` が追加済みであること。

6. `handleSendToExistingDialogue` を本実装に差し替える:

```typescript
const handleSendToExistingDialogue = useCallback((dialogueText: string) => {
  // 既存 DialogueNode の最初の 1 個を探す
  const dialogueNode = getNodes().find(n => n.type === 'dialogue');
  if (!dialogueNode) return;

  const allDialogueNodes = getNodes().filter(n => n.type === 'dialogue');
  if (allDialogueNodes.length > 1) {
    // 複数存在: 警告 Toast (既存 Toast 実装があれば使用、なければ console.warn)
    console.warn('複数の DialogueNode が見つかりました。最初の 1 個に転記します。');
    // TODO: Toast 実装がある場合はそちらを呼ぶ
  }

  // 既存 nodeDataUpdate CustomEvent パターンを再利用
  const updateEvent = new CustomEvent('nodeDataUpdate', {
    detail: { nodeId: dialogueNode.id, updates: { text: dialogueText } },
  });
  window.dispatchEvent(updateEvent);

  // dismiss 記録
  dismissedDialogueHashRef.current = normalizeDialogue(dialogueText);
  setPendingDialogue(null);
}, [getNodes, normalizeDialogue]);
```

7. ファイルを保存する

### NodeEditor.tsx の変更

8. `NodeEditor.tsx` の既存 `nodeDataUpdate` listener の近くを確認する:

```bash
grep -n "nodeDataUpdate\|addEventListener\|useEffect" \
  /Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/NodeEditor.tsx | head -30
```

9. `createDialogueNodeFromPrompt` listener を追加する。`setNodes` の関数形式 setter を使うことで closure 問題を回避する (N2 対応):

```typescript
// NodeEditor.tsx の NodeEditorInner 関数内に追加
// (既存の nodeDataUpdate listener useEffect の近くに配置)
useEffect(() => {
  const handler = (e: Event) => {
    const event = e as CustomEvent<{ sourcePromptNodeId: string; initialText: string }>;
    // setNodes の関数形式 setter で最新の nodes にアクセス (closure 問題回避)
    setNodes((prevNodes) => {
      const sourceNode = prevNodes.find(n => n.id === event.detail.sourcePromptNodeId);
      if (!sourceNode) return prevNodes;

      const newNode = {
        id: `dialogue-${Date.now()}`,
        type: 'dialogue',
        position: {
          x: sourceNode.position.x + 320,
          y: sourceNode.position.y + 200,
        },
        data: createData('dialogue', { text: event.detail.initialText }),
      };
      return [...prevNodes, newNode];
      // 注: PromptNode → DialogueNode のエッジは自動接続しない
    });
  };

  window.addEventListener('createDialogueNodeFromPrompt', handler);
  return () => window.removeEventListener('createDialogueNodeFromPrompt', handler);
  // N2 対応: 依存配列は setNodes のみ (関数形式 setter で nodes 依存を排除)
}, [setNodes]);
```

注意: `createData` は `NodeEditor.tsx` L45 で import 済みの `createDefaultNodeData as createData`。

10. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1 | head -30
npm run lint 2>&1 | head -30
```

型エラーなし、lint エラーなし。

動作確認 (手動):
- PromptNode にセリフ含む日本語を入力 → 確認カード表示
- 「新規 DialogueNode を作成」ボタンをクリック → NodeEditor にノードが 1 個追加される (AC-C5)
- 新規 DialogueNode の `text` フィールドに extracted_dialogue が入っている
- 位置が PromptNode の右下 (x+320, y+200)
- 「既存ノードに転記」ボタンをクリック → 既存 DialogueNode の text が更新される (AC-C6)
- 「無視」後のカード表示後は `dismissedDialogueHashRef` で suppress される (T2-4 との連携)

```bash
# CustomEvent 名の確認
grep -rn "createDialogueNodeFromPrompt" \
  /Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/
```

## 完了条件

- `handleCreateDialogueNode` が `createDialogueNodeFromPrompt` CustomEvent を発火する (AC-C5)
- `handleSendToExistingDialogue` が `nodeDataUpdate` CustomEvent を発火する (AC-C6)
- NodeEditor の listener が `setNodes` 関数形式 setter でノードを追加する (N2)
- 新規 DialogueNode の位置が `sourceNode.position + {x:320, y:200}` (AC-C5)
- 既存 DialogueNode がない場合は `handleSendToExistingDialogue` が no-op
- `npx tsc --noEmit` エラーなし

## コミットメッセージ案

```
feat(PromptNode,NodeEditor): implement createDialogueNodeFromPrompt CustomEvent flow

N2/N4: PromptNode dispatches createDialogueNodeFromPrompt event.
NodeEditor listens with functional setNodes to avoid stale closure.
handleSendToExistingDialogue reuses existing nodeDataUpdate pattern.
New DialogueNode positioned at sourceNode + {x:320, y:200} (AC-C5).
```
