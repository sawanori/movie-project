# T1-8: BE 単体テスト追加 (10 ケース)

## 依存タスク

T1-7 完了後

## 目的

`tests/external/test_gemini_translate_story.py` を新規作成し、AC-A1/B3/B4/C1-C9/D1-D2 のバックエンド側 10 ケースをカバーする。`_run_gemini_translation` を monkeypatch することで Gemini API を呼ばずに高速かつ決定論的なテストを実現する (N6 対応)。

## 対象ファイル

- 新規作成: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external/test_gemini_translate_story.py`
- 確認: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external/` ディレクトリの存在 (なければ `__init__.py` も作成)

## 実装手順

1. テストディレクトリを確認する:

```bash
ls /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/
```

`external/` がなければ作成:

```bash
mkdir -p /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external
touch /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external/__init__.py
```

2. 以下のテストファイルを作成する:

```python
"""
tests/external/test_gemini_translate_story.py

translate_story_prompt / _extract_prompt_components / _extract_dialogues_via_regex
/ _sanitize_reserve_typo の単体テスト。

N6 対応: Gemini API 実呼び出しを避けるため _run_gemini_translation を
monkeypatch で置き換える。asyncio.to_thread の mock は _extract_prompt_components
のフォールバックパスのテストでのみ使用する。
"""
import json
import pytest
from unittest.mock import AsyncMock, patch, MagicMock

from app.external.gemini_client import (
    translate_story_prompt,
    TranslateStoryPromptInput,
    ExtractedComponents,
    _extract_prompt_components,
    _extract_dialogues_via_regex,
    _sanitize_reserve_typo,
)


# ---- ヘルパー ----

def _make_ai_response(data: dict) -> MagicMock:
    """asyncio.to_thread が返す Gemini レスポンス風 mock。"""
    m = MagicMock()
    m.text = json.dumps(data)
    return m


def _extraction_mock(
    subject_visual: str = "test subject",
    action: str = "move",
    camera: str = "",
    dialogue: str = "",
    micro_expression: str = "",
    lighting: str = "",
    other: str = "",
    must_include: str = "",
):
    """AI 抽出レスポンス用 asyncio.to_thread mock。"""
    return AsyncMock(return_value=_make_ai_response({
        "subject_visual": subject_visual,
        "action": action,
        "camera": camera,
        "dialogue": dialogue,
        "micro_expression": micro_expression,
        "lighting": lighting,
        "other": other,
        "must_include": must_include,
    }))


# ============================================================
# 1. typo サニタイズ: 純関数テスト (AC-A1)
# ============================================================

def test_reserve_to_preserve_sanitization():
    """AC-A1: _sanitize_reserve_typo が Reserve → Preserve に置換する。"""
    text = "Reserve exact appearance from reference. Reserve the subject identity."
    cleaned = _sanitize_reserve_typo(text)
    assert "Reserve" not in cleaned
    assert "Preserve exact appearance" in cleaned
    assert "Preserve the subject" in cleaned


def test_sanitize_does_not_affect_other_reserve_words():
    """_sanitize_reserve_typo は固定フレーズのみ置換し、無関係な "reserve" は触れない。"""
    text = "Reserve a table. Preserve exact appearance."
    cleaned = _sanitize_reserve_typo(text)
    # "Reserve a table" は置換対象外
    assert "Reserve a table" in cleaned
    # "Preserve exact appearance" は保持
    assert "Preserve exact appearance" in cleaned


# ============================================================
# 2. 正規表現セリフ抽出: 純関数テスト (AC-C1 〜 C9)
# ============================================================

def test_extract_dialogues_single_kagi():
    """AC-C1: 「」内の単一セリフを抽出する。"""
    result = _extract_dialogues_via_regex("キャラクターが「ちょっとまって…」と言う")
    assert result == "ちょっとまって…"


def test_extract_dialogues_single_double_kagi():
    """AC-C1 (二重カッコ): 『』内の単一セリフを抽出する。"""
    result = _extract_dialogues_via_regex("キャラが『こんにちは』と言う")
    assert result == "こんにちは"


def test_extract_dialogues_multiple():
    """AC-C2: 複数セリフを改行区切りで結合する。"""
    result = _extract_dialogues_via_regex("「あ…」と言ってから「やめて…」と続ける")
    assert result is not None
    assert "あ…" in result
    assert "やめて…" in result
    assert "\n" in result


def test_extract_dialogues_none_when_no_brackets():
    """AC-C3: セリフカッコがない場合 None を返す。"""
    result = _extract_dialogues_via_regex("女性が振り向く")
    assert result is None


def test_extract_dialogues_empty_brackets_returns_none():
    """10-5 対応: 空の「」は除外して None を返す。"""
    result = _extract_dialogues_via_regex("キャラクターが「」と言う")
    assert result is None


def test_extract_dialogues_nested_brackets_ac_c8():
    """AC-C8: 異種ネストカッコ「彼は『やめて』と叫んだ」の挙動を固定。

    採用案 a: 別々抽出後に結合。重複は致命的でない。
    """
    result = _extract_dialogues_via_regex("「彼は『やめて』と叫んだ」")
    assert result is not None
    assert "彼は『やめて』と叫んだ" in result
    assert "やめて" in result
    assert "\n" in result


# ============================================================
# 3. _extract_prompt_components のフォールバック (N6)
# ============================================================

@pytest.mark.asyncio
async def test_extract_components_regex_fallback_on_ai_failure(monkeypatch):
    """N6 対応: AI 抽出失敗時に正規表現フォールバックでセリフが取れる。"""
    monkeypatch.setattr(
        "app.external.gemini_client.asyncio.to_thread",
        AsyncMock(side_effect=Exception("Gemini timeout")),
    )

    extracted = await _extract_prompt_components("キャラが「セリフ」と言う")
    assert extracted.dialogue == "セリフ"
    assert isinstance(extracted, ExtractedComponents)
    # フォールバック時は description_ja が subject_visual に入る
    assert "キャラが" in extracted.subject_visual or extracted.subject_visual != ""


@pytest.mark.asyncio
async def test_extract_components_regex_priority_over_ai(monkeypatch):
    """10-8 対応: 正規表現が取れた場合は AI の dialogue より優先される。"""
    # AI は空の dialogue を返すが、正規表現では取れる入力
    monkeypatch.setattr(
        "app.external.gemini_client.asyncio.to_thread",
        _extraction_mock(dialogue=""),  # AI は空
    )
    extracted = await _extract_prompt_components("「テスト」と言う")
    # 正規表現で「テスト」が取れるので AI の空を無視
    assert extracted.dialogue == "テスト"


# ============================================================
# 4. translate_story_prompt 統合 (Gemini mock)
# ============================================================

@pytest.mark.asyncio
async def test_translate_story_prompt_object_excludes_person_phrases(monkeypatch):
    """AC-B3 / AC-C9: subject_type='object' で人物前提フレーズが混入しない。"""
    monkeypatch.setattr(
        "app.external.gemini_client.asyncio.to_thread",
        _extraction_mock(subject_visual="beige knit sweater, no limbs"),
    )
    # _run_gemini_translation は実際の Gemini を呼ばないよう mock
    with patch(
        "app.external.gemini_client._run_gemini_translation",
        new=AsyncMock(
            return_value=(
                "Preserve object design. CLIP SPECIFIC:\n"
                "Subject: Beige knit sweater, no limbs, hung on hanger\n"
                "Action: Slight forward tilt\n"
                "Camera: Static\n"
                "Must include: natural knit fiber movement"
            )
        ),
    ):
        params = TranslateStoryPromptInput(
            description_ja="ベージュのニットセーター (手足なし)",
            subject_type="object",
        )
        result_en, dialogue = await translate_story_prompt(params)

    assert "Same face" not in result_en
    assert "Same hair" not in result_en
    assert "Same clothing" not in result_en
    assert dialogue is None


@pytest.mark.asyncio
async def test_translate_story_prompt_with_dialogue_extracted(monkeypatch):
    """AC-C1 統合: セリフが extracted_dialogue に返り、english_prompt 本文に混入しない。"""
    monkeypatch.setattr(
        "app.external.gemini_client.asyncio.to_thread",
        _extraction_mock(subject_visual="character", dialogue="ちょっとまって…"),
    )
    with patch(
        "app.external.gemini_client._run_gemini_translation",
        new=AsyncMock(
            return_value=(
                "Preserve character design. CLIP SPECIFIC:\n"
                "Subject: Character\nAction: Speak\nCamera: Static\n"
                "Must include: natural motion, subtle lip-sync motion"
            )
        ),
    ):
        params = TranslateStoryPromptInput(
            description_ja="キャラクターが「ちょっとまって…」と言う",
        )
        result_en, dialogue = await translate_story_prompt(params)

    # セリフは extracted_dialogue に返る
    assert dialogue == "ちょっとまって…"
    # 本文に日本語セリフが混入しない
    assert "ちょっとまって" not in result_en


@pytest.mark.asyncio
async def test_translate_story_prompt_act_two_phase1_warning_only(monkeypatch, caplog):
    """AC-B4: use_act_two=True でも 500 にならず warning ログのみ。"""
    import logging
    monkeypatch.setattr(
        "app.external.gemini_client.asyncio.to_thread",
        _extraction_mock(),
    )
    with patch(
        "app.external.gemini_client._run_gemini_translation",
        new=AsyncMock(return_value="Preserve identity. CLIP SPECIFIC: Action: turn."),
    ):
        params = TranslateStoryPromptInput(
            description_ja="女性が振り向く",
            subject_type="person",
            use_act_two=True,
            motion_type="natural",
            expression_intensity=4,
            body_control=True,
        )
        with caplog.at_level(logging.WARNING, logger="app.external.gemini_client"):
            result_en, _ = await translate_story_prompt(params)

    assert isinstance(result_en, str)
    assert len(result_en) > 0
    assert any("Act-Two mode is not supported" in r.message for r in caplog.records)


@pytest.mark.asyncio
async def test_translate_story_prompt_sanitizes_reserve_typo(monkeypatch):
    """AC-A1 統合: Gemini が Reserve を誤生成しても sanitize で Preserve に変換。"""
    monkeypatch.setattr(
        "app.external.gemini_client.asyncio.to_thread",
        _extraction_mock(),
    )
    with patch(
        "app.external.gemini_client._run_gemini_translation",
        # AI が typo を返すシミュレーション
        new=AsyncMock(return_value="Reserve exact appearance from reference."),
    ):
        params = TranslateStoryPromptInput(description_ja="テスト入力")
        result_en, _ = await translate_story_prompt(params)

    assert "Reserve exact appearance" not in result_en
    assert "Preserve exact appearance" in result_en
```

3. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate

pytest tests/external/test_gemini_translate_story.py -v
```

期待: 全 10 ケースが PASSED。

```bash
# 既存テストへの影響確認
pytest tests/ -v -q \
  --ignore=tests/videos/test_text_to_image.py \
  --ignore=tests/library/test_service.py
```

## 完了条件

- `tests/external/test_gemini_translate_story.py` が存在し、10 ケース全 PASSED
- Gemini API 実呼び出しなし (全テストが mock ベース)
- AC-A1 (typo サニタイズ) / AC-B3 (人物フレーズ除外) / AC-B4 (Act-Two 警告) / AC-C1〜C9 (セリフ抽出) が検証されている
- 既存テストに新規失敗なし

## コミットメッセージ案

```
test(translate): add 10 unit tests for translate_story_prompt BE functions

Covers AC-A1 typo sanitization, AC-B3/B4 subject_type and Act-Two,
AC-C1-C9 dialogue extraction edge cases. All tests use Gemini mocks
(N6) to avoid real API calls.
```
