# T1-4: _build_translate_system_prompt / _build_reference_instruction / _sanitize_reserve_typo 追加

## 依存タスク

T1-1 完了後 (T1-2/T1-3 とは並列着手可)

## 目的

B 案テンプレート軽量化 (4 必須 + 3 オプション、1200 文字目標) を実現する 3 関数を実装する。`_build_reference_instruction` が `subject_type` 分岐で人物前提フレーズを除外し、`_build_translate_system_prompt` が新テンプレ構造を組み立て、`_sanitize_reserve_typo` が A 案の typo 安全網を提供する。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/external/gemini_client.py`
  - `_extract_prompt_components` の後に 3 関数を追加

## 実装手順

1. 以下の 3 関数を `_extract_prompt_components` の直後に追加する:

```python
def _build_reference_instruction(subject_type: str) -> str:
    """B2 / B3 対応: subject_type に応じた Reference Instruction 1 行を返す。

    subject_type='object' / 'animation' 時は人物前提フレーズ
    ("Same face, same hair, same clothing") を除外した表現を使用。
    """
    if subject_type == "person":
        return (
            "Preserve subject's identity, facial features, outfit, and pose "
            "from source image. Maintain the exact character appearance."
        )
    if subject_type == "object":
        return (
            "Preserve subject design, materials, configuration, and proportions "
            "from source image. Maintain the exact object appearance."
        )
    if subject_type == "animation":
        return (
            "Preserve character design, art style, color palette, and visual "
            "features from source image. Maintain the exact stylistic identity."
        )
    # フォールバック (未知の subject_type)
    return (
        "Preserve the visual identity and characteristics from source image."
    )


def _build_translate_system_prompt(
    extracted: ExtractedComponents,
    reference_instruction: str,
    params: TranslateStoryPromptInput,
) -> str:
    """システムプロンプト構築。ExtractedComponents の str 統一に依存。

    B3 対応: extracted.* は常に str。.strip() 直接呼び出し可能で None ガード不要。
    A 案: CRITICAL RULES に "Preserve" typo 禁止を明示 (2 層防御の第 1 層)。
    B 案: 必須 4 軸 + オプション 3 軸の新テンプレ (1200 文字目標)。
    """
    # 既に ExtractedComponents は str 統一なので .strip() で空判定のみ
    subject_visual_hint = extracted.subject_visual.strip()
    action_hint = extracted.action.strip()
    camera_hint = (params.camera_work or extracted.camera).strip()
    expression_hint = extracted.micro_expression.strip()
    lighting_hint = extracted.lighting.strip()
    has_dialogue = extracted.dialogue is not None  # 唯一 Optional[str]

    # オプション軸の構築 (該当する記述があるときのみ含める)
    optional_lines = []
    if expression_hint:
        optional_lines.append(f"Micro-expression: {{translate: {expression_hint}}}")
    if lighting_hint:
        optional_lines.append(f"Lighting: {{translate: {lighting_hint}}}")
    if has_dialogue:
        optional_lines.append(
            "Must include (in addition): subtle lip-sync motion as if "
            "the subject is speaking. Do NOT include the dialogue text itself."
        )

    optional_block = "\n".join(optional_lines) if optional_lines else ""

    return f"""\
You are a prompt engineer for video generation AI ({params.video_provider}).

GOAL: Convert the Japanese description into a compact English prompt
that the model can execute reliably. Follow the structure below EXACTLY.

CRITICAL RULES:
1. Use "Preserve" (with P), NEVER use "Reserve" (forbidden typo).
2. The Subject field MUST include ALL visual attributes the user provided
   (colors, materials, shape, distinctive features). DO NOT omit them as
   "inheritable from image" — they are required for the model to anchor.
3. Keep total length under 1200 characters. Be concise but specific.
4. Subject type is "{params.subject_type}". Do NOT use human-specific phrases
   (e.g. "Same face, same hair, same clothing") unless subject_type is "person".
5. Dialogue lines (extracted separately) MUST NOT appear in the prompt body.
   Only the meta-instruction "subtle lip-sync motion" is allowed.

OUTPUT TEMPLATE (use this EXACT structure):

{reference_instruction}

CLIP SPECIFIC:
Subject: {{translate to English, include ALL visual attributes from: {subject_visual_hint}}}
Action: {{translate to English: {action_hint}}}
Camera: {{translate to English or use as-is: {camera_hint or "natural framing"}}}
Must include: {{natural motion, subtle physics appropriate to the subject}}
{optional_block}

OUTPUT: Return ONLY the prompt text following the template above.
No explanations, no quotes, no markdown code fences.
"""


def _sanitize_reserve_typo(text: str) -> str:
    """A 案安全網 (第 2 層): Reserve → Preserve に置換 (固定フレーズのみ)。

    Gemini が "Reserve" を誤生成した場合の最終防御。
    特定フレーズのみ置換することで意図しない書き換えを避ける。
    """
    return (
        text
        .replace("Reserve exact appearance", "Preserve exact appearance")
        .replace("Reserve the subject", "Preserve the subject")
        .replace("Reserve the source", "Preserve the source")
    )
```

2. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate

python -c "
from app.external.gemini_client import (
    _build_reference_instruction,
    _sanitize_reserve_typo,
    ExtractedComponents,
    TranslateStoryPromptInput,
)

# subject_type 分岐確認
person_instr = _build_reference_instruction('person')
assert 'facial features' in person_instr
assert 'outfit' in person_instr

object_instr = _build_reference_instruction('object')
assert 'materials' in object_instr
assert 'facial features' not in object_instr  # 人物前提フレーズ不在

animation_instr = _build_reference_instruction('animation')
assert 'art style' in animation_instr

# typo サニタイズ
result = _sanitize_reserve_typo('Reserve exact appearance from reference. Reserve the subject.')
assert 'Reserve' not in result
assert 'Preserve exact appearance' in result
assert 'Preserve the subject' in result

print('_build_reference_instruction and _sanitize_reserve_typo OK')
"
```

`_build_translate_system_prompt` の動作確認:

```bash
python -c "
from app.external.gemini_client import (
    _build_translate_system_prompt,
    _build_reference_instruction,
    ExtractedComponents,
    TranslateStoryPromptInput,
)

extracted = ExtractedComponents(
    subject_visual='beige knit sweater',
    action='slight forward tilt',
    camera='',
    micro_expression='bewilderment',
    lighting='',
    other='',
    must_include='',
    dialogue='ちょっとまって',
)
params = TranslateStoryPromptInput(description_ja='テスト', subject_type='object')
ref = _build_reference_instruction('object')
prompt = _build_translate_system_prompt(extracted, ref, params)

assert 'Preserve' in prompt
assert 'Reserve' not in prompt  # typo 記述が混入していないこと
assert 'beige knit sweater' in prompt
assert 'Same face' not in prompt
assert 'lip-sync' in prompt  # dialogue あり → オプション行追加
assert len(prompt) < 3000  # システムプロンプト自体は 1200 文字制限対象外
print('_build_translate_system_prompt OK')
"
```

## 完了条件

- `_build_reference_instruction('object')` で "Same face/hair/clothing" 系フレーズが含まれない (AC-B3 対応)
- `_sanitize_reserve_typo` が固定フレーズのみ置換する (AC-A1 第 2 層)
- `_build_translate_system_prompt` がシステムプロンプト文字列を返す
- セリフあり時に "lip-sync" 指示がオプション行に含まれる
- セリフなし時にオプション行が追加されない
- 既存テストが壊れていない

## コミットメッセージ案

```
feat(translate): add _build_translate_system_prompt and subject_type reference instructions

Implements B-plan lightweight template (4 required + 3 optional axes, 1200 char target).
_build_reference_instruction provides subject_type-conditional phrases excluding
human-specific "Same face/hair/clothing" for object/animation types.
_sanitize_reserve_typo adds A-plan second-layer typo guard.
```
