# T1-5: translate_story_prompt メイン関数実装

## 依存タスク

T1-1, T1-2, T1-3, T1-4 が全て完了後

## 目的

`translate_story_prompt(params: TranslateStoryPromptInput) -> tuple[str, Optional[str]]` を実装し、A/B/C 案を統合する。構造化抽出 → テンプレ整形 → 翻訳実行 → typo サニタイズの 3 フェーズを組み合わせ、`(english_prompt, extracted_dialogue)` を返す。Act-Two 引数 (`use_act_two=True`) は Phase 1 では警告ログのみ (B4 対応)。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/external/gemini_client.py`
  - `_sanitize_reserve_typo` の後に `translate_story_prompt` メイン関数を追加

## 実装手順

1. 以下の関数を追加する:

```python
async def translate_story_prompt(
    params: TranslateStoryPromptInput,
) -> tuple[str, Optional[str]]:
    """PromptNode 用: 日本語プロンプトを英語に翻訳し、セリフを分離抽出する。

    Args:
        params: 翻訳パラメータ (dataclass で集約)

    Returns:
        tuple[english_prompt: str, extracted_dialogue: Optional[str]]
        - english_prompt: 常に str (失敗時は例外送出、空文字は返さない)
        - extracted_dialogue: 「」/『』検出時のみ str、未検出時 None

    Raises:
        Exception: Gemini 翻訳呼び出し失敗時 (フォールバックなし、呼び出し元でハンドリング)
    """
    # B4 対応: Phase 1 では Act-Two 引数を未使用、警告ログのみ
    if params.use_act_two:
        logger.warning(
            "Act-Two mode is not supported in translate_story_prompt yet. "
            "Phase 2 will add _build_act_two_instruction(). "
            "Falling back to standard translation for now. "
            "use_act_two=%s motion_type=%s expression_intensity=%s body_control=%s",
            params.use_act_two,
            params.motion_type,
            params.expression_intensity,
            params.body_control,
        )

    # --- Phase 1: 構造化抽出 ---
    extracted: ExtractedComponents = await _extract_prompt_components(
        params.description_ja
    )

    # --- Phase 2: 翻訳 + テンプレ整形 ---
    reference_instruction = _build_reference_instruction(params.subject_type)
    system_prompt = _build_translate_system_prompt(
        extracted=extracted,
        reference_instruction=reference_instruction,
        params=params,
    )

    english_prompt = await _run_gemini_translation(system_prompt, params.description_ja)

    # --- Phase 3: typo サニタイズ (A 案安全網、第 2 層) ---
    english_prompt = _sanitize_reserve_typo(english_prompt)

    # B5 対応: 1200 文字超過時は warning ログのみ (ハード上限なし)
    if len(english_prompt) > 1200:
        logger.warning(
            "translate_story_prompt output exceeded soft target: %d chars "
            "(target <= 1200). Kling 2500-char truncate acts as hard limit.",
            len(english_prompt),
        )

    return english_prompt, extracted.dialogue
```

2. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate

# import 確認
python -c "
from app.external.gemini_client import translate_story_prompt, TranslateStoryPromptInput
import inspect
assert inspect.iscoroutinefunction(translate_story_prompt)
print('translate_story_prompt import OK')
"
```

monkeypatch を使った結合動作確認 (Gemini API を呼ばずに全パスが通ること):

```bash
python -c "
import asyncio
from unittest.mock import AsyncMock, patch
from app.external.gemini_client import translate_story_prompt, TranslateStoryPromptInput

async def test():
    params = TranslateStoryPromptInput(
        description_ja='キャラクターが「こんにちは」と言う',
        subject_type='object',
    )
    # _run_gemini_translation と _extract_prompt_components 内部の to_thread を mock
    with patch('app.external.gemini_client._run_gemini_translation',
               new=AsyncMock(return_value='Preserve object design. CLIP SPECIFIC: ...')):
        with patch('app.external.gemini_client.asyncio.to_thread',
                   new=AsyncMock(return_value=type('R', (), {'text': '{\"subject_visual\": \"test\", \"action\": \"greet\", \"camera\": \"\", \"dialogue\": \"\", \"micro_expression\": \"\", \"lighting\": \"\", \"other\": \"\", \"must_include\": \"\"}'})())) :
            en, dialogue = await translate_story_prompt(params)
            assert isinstance(en, str)
            assert len(en) > 0
            # dialogue は正規表現で「こんにちは」を取得しているはず
            assert dialogue == 'こんにちは'
            print(f'translate_story_prompt OK: en={en[:50]}..., dialogue={dialogue}')

asyncio.run(test())
"
```

## 完了条件

- `translate_story_prompt` が `async def` として定義されている
- `use_act_two=True` 時に例外を出さず `logger.warning` だけ出力して翻訳が続行する (AC-B4)
- 戻り値が `tuple[str, Optional[str]]` 型
- `extracted.dialogue` が `None` の場合 `(english_prompt, None)` を返す
- 1200 文字超過時に `logger.warning` を出力し、HTTP レスポンスは正常
- 既存テストが壊れていない

## コミットメッセージ案

```
feat(translate): implement translate_story_prompt main function

Integrates A/B/C plan improvements: 2-layer Reserve typo guard,
subject_type-conditional template, dialogue extraction via
_extract_prompt_components. Act-Two args logged with warning in Phase 1.
Soft 1200-char target enforced via warning log only.
```
