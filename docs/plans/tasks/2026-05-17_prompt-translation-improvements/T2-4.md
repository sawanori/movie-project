# T2-4: dismissedDialogueHashRef + normalizeDialogue 実装

## 依存タスク

T2-1 完了後 (T2-2/T2-3 とは並列着手可、ただし T2-3 のマージ後に編集すること)

## 目的

「無視」ボタンクリック後に同一セリフが再翻訳されても確認カードが再表示されないよう、正規化ハッシュによる dismiss 抑制を実装する (N1/N3 対応)。`useRef` で ref を管理することで `useEffect` 依存配列への混入を防ぐ。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/PromptNode.tsx`
  - `useRef` の import 追加
  - `dismissedDialogueHashRef` の追加
  - `normalizeDialogue` 関数の追加
  - `useEffect` の `extracted_dialogue` 判定にハッシュ比較を追加
  - 「無視」ボタンの `onClick` を dismiss 記録ロジックに差し替え

## 実装手順

前提: T2-3 の変更がマージ済みであること。

1. `PromptNode.tsx` の import に `useRef` を追加する:

```typescript
import { useCallback, useState, useEffect, useRef } from 'react';
```

2. `pendingDialogue` state の直後に `dismissedDialogueHashRef` を追加する:

```typescript
// N1 対応: dismissed ハッシュは useRef 管理 (依存配列に含めず useEffect 再走を防ぐ)
const dismissedDialogueHashRef = useRef<string | null>(null);
```

3. コンポーネント内に `normalizeDialogue` 関数を追加する:

```typescript
// N3 対応: セリフ正規化 (空白/改行を統一して同一性判定)
const normalizeDialogue = (raw: string): string =>
  raw.trim().replace(/\s+/g, ' ');
```

4. `useEffect` 内の `extracted_dialogue` ハンドリング部分を dismiss 判定に更新する:

```typescript
// T2-3 の単純 setPendingDialogue から更新:
if (result.extracted_dialogue) {
  const normalizedHash = normalizeDialogue(result.extracted_dialogue);
  // N3 対応: dismiss 済みの正規化ハッシュと同一なら再表示しない
  if (normalizedHash !== dismissedDialogueHashRef.current) {
    setPendingDialogue(result.extracted_dialogue);
  }
} else {
  setPendingDialogue(null);
}
```

5. 「無視」ボタンの `onClick` を dismiss 記録ロジックに更新する (T2-3 で追加したボタン):

```typescript
// 変更前 (T2-3):
onClick={() => {
  setPendingDialogue(null);
}}

// 変更後 (T2-4):
onClick={() => {
  // N3 対応: 正規化ハッシュで dismiss 記録
  dismissedDialogueHashRef.current = normalizeDialogue(pendingDialogue);
  setPendingDialogue(null);
}}
```

6. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1 | head -30
npm run lint 2>&1 | head -30
```

型エラーなし、lint エラーなし。

動作確認 (手動):
- PromptNode に「`「こんにちは」`」を入力 → 確認カード表示
- 「無視」をクリック → カード非表示
- プロンプトを一文字変えて戻す (同じセリフが含まれる) → カードが再表示**されない**こと
- プロンプトを全く別の文に変えてセリフを削除 → `setPendingDialogue(null)` で非表示
- 新しいセリフ「`「さようなら」`」に変更 → 新しいカードが表示されること

## 完了条件

- `dismissedDialogueHashRef` が `useRef<string | null>(null)` として定義されている
- `normalizeDialogue` が `trim().replace(/\s+/g, ' ')` を実行する
- `useEffect` の依存配列に `dismissedDialogueHashRef` が含まれていない (ref は配列不要)
- 「無視」後に同一セリフで再翻訳してもカードが出ない (N3 対応)
- 異なるセリフが来た場合はカードが出る

## コミットメッセージ案

```
feat(PromptNode): add dismissedDialogueHashRef to suppress re-showing same dialogue

N1/N3: useRef prevents stale closure issues in useEffect deps.
normalizeDialogue collapses whitespace for consistent identity comparison.
Dismiss hash is recorded on "無視" click and checked before showing card.
```
