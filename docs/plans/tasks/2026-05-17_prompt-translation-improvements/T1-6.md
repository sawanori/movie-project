# T1-6: TranslateStoryPromptResponse に extracted_dialogue フィールド追加

## 依存タスク

T1-1 完了後 (T1-2/T1-3/T1-4 とは並列着手可)

## 目的

`schemas.py` の `TranslateStoryPromptResponse` に `extracted_dialogue: str | None` フィールドを追加する。既存の `english_prompt` フィールドは維持するため追加フィールドのみの変更で後方互換性を保つ (Interface Change Matrix 参照)。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/videos/schemas.py`
  - L432-434 の `TranslateStoryPromptResponse` クラス定義を変更

## 実装手順

1. `schemas.py` の L432-434 を確認する (現在の定義):

```python
class TranslateStoryPromptResponse(BaseModel):
    """シーン動画プロンプト翻訳レスポンス"""
    english_prompt: str = Field(..., description="英語プロンプト（テンプレート適用済み）")
```

2. `extracted_dialogue` フィールドを追加する:

```python
class TranslateStoryPromptResponse(BaseModel):
    """シーン動画プロンプト翻訳レスポンス"""
    english_prompt: str = Field(
        ..., description="英語プロンプト (テンプレート適用済み)"
    )
    extracted_dialogue: str | None = Field(
        default=None,
        description=(
            "日本語入力から自動抽出されたセリフ (「」/『』内のテキスト)。"
            "複数セリフは改行で結合。検出なしの場合は null。"
        ),
    )
```

注意: `str | None` は Python 3.10+ 構文。Python 3.11+ ターゲットのため問題なし。

3. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
source venv/bin/activate

python -c "
from app.videos.schemas import TranslateStoryPromptResponse

# フィールド確認
r = TranslateStoryPromptResponse(english_prompt='Preserve design.')
assert r.english_prompt == 'Preserve design.'
assert r.extracted_dialogue is None  # デフォルト None

r2 = TranslateStoryPromptResponse(
    english_prompt='Preserve design.',
    extracted_dialogue='ちょっとまって',
)
assert r2.extracted_dialogue == 'ちょっとまって'

# JSON シリアライズ確認
import json
data = json.loads(r.model_dump_json())
assert 'extracted_dialogue' in data
assert data['extracted_dialogue'] is None

print('TranslateStoryPromptResponse OK')
"
```

Swagger UI での確認 (開発サーバー起動時):
- `http://localhost:8000/docs` で `POST /api/v1/videos/story/translate` のレスポンス schema に `extracted_dialogue` が表示されること

## 完了条件

- `TranslateStoryPromptResponse` に `extracted_dialogue: str | None` フィールドが追加されている
- デフォルト値が `None` (旧コードの `extracted_dialogue` 未指定呼び出しとの後方互換)
- `english_prompt` フィールドが変更されていない
- Pydantic の `model_dump_json()` で `extracted_dialogue` が JSON に含まれる
- 既存テストが壊れていない

## コミットメッセージ案

```
feat(schemas): add extracted_dialogue field to TranslateStoryPromptResponse

Backward-compatible addition: existing callers ignoring the new field
are unaffected. extracted_dialogue is str | None (null when no Japanese
dialogue brackets detected).
```
