# T2-1: API クライアント型定義拡張

## 依存タスク

Phase 1 (T1-1〜T1-8) 全完了後 (Phase 2 最初のタスク)

## 目的

`lib/api/client.ts` の `translateStoryPrompt` 型を拡張し、`TranslateStoryPromptRequest` インターフェースを名前付き export として追加し、戻り値型に `extracted_dialogue: string | null` を加える。T1-6 で確定した BE schema と FE 型定義を同期させる。

## 対象ファイル

- 編集: `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/lib/api/client.ts`
  - L202-219 の `translateStoryPrompt` メソッド定義を変更

## 実装手順

1. `client.ts` の L202-219 を確認する (現在の定義):

```typescript
// 日本語プロンプトを英語に翻訳（テンプレート適用）
translateStoryPrompt: (data: {
  description_ja: string;
  video_provider?: 'runway' | 'veo' | 'domoai' | 'piapi_kling' | 'hailuo' | 'seedance';
  subject_type?: 'person' | 'object' | 'animation';
  camera_work?: string;
  animation_category?: '2d' | '3d' | null;
  animation_template?: string | null;
  use_act_two?: boolean;
  motion_type?: string | null;
  expression_intensity?: number;
  body_control?: boolean;
}): Promise<{ english_prompt: string }> =>
  fetchWithAuth("/api/v1/videos/story/translate", {
    method: "POST",
    body: JSON.stringify(data),
  }),
```

2. `translateStoryPrompt` の前に 2 つのインターフェースを追加し、メソッドの型を更新する:

```typescript
// B2 対応: TranslateStoryPromptRequest 型を明示 export (PromptNode から import 可能に)
export interface TranslateStoryPromptRequest {
  description_ja: string;
  video_provider?: 'runway' | 'veo' | 'domoai' | 'piapi_kling' | 'hailuo' | 'seedance';
  subject_type?: 'person' | 'object' | 'animation';
  camera_work?: string;
  animation_category?: '2d' | '3d' | null;
  animation_template?: string | null;
  use_act_two?: boolean;
  motion_type?: string | null;
  expression_intensity?: number;
  body_control?: boolean;
}

export interface TranslateStoryPromptResponse {
  english_prompt: string;
  extracted_dialogue: string | null;  // C 案: セリフ抽出フィールド追加
}

// 日本語プロンプトを英語に翻訳（テンプレート適用）
translateStoryPrompt: (
  data: TranslateStoryPromptRequest,
): Promise<TranslateStoryPromptResponse> =>
  fetchWithAuth("/api/v1/videos/story/translate", {
    method: "POST",
    body: JSON.stringify(data),
  }),
```

注意: インターフェースは `videosApi` オブジェクト定義の**外側** (モジュールトップレベル) に配置し、`export` する。`fetchWithAuth` の呼び出し内容は変更しない。

3. ファイルを保存する

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1 | head -30
```

型エラーが出ないことを確認する。

```bash
# インターフェースが import できることを確認
node -e "
const ts = require('typescript');
const src = require('fs').readFileSync('./lib/api/client.ts', 'utf-8');
const has = src.includes('export interface TranslateStoryPromptRequest') &&
            src.includes('export interface TranslateStoryPromptResponse') &&
            src.includes('extracted_dialogue: string | null');
console.log('Interfaces added:', has);
"
```

## 完了条件

- `TranslateStoryPromptRequest` が named export として存在する
- `TranslateStoryPromptResponse` が `extracted_dialogue: string | null` フィールドを持つ
- `translateStoryPrompt` の戻り値型が `Promise<TranslateStoryPromptResponse>` になっている
- `npx tsc --noEmit` でエラーなし
- 既存の `npm run test` が通る

## コミットメッセージ案

```
chore(types): extend TranslateStoryPromptResponse with extracted_dialogue

Adds named exports for TranslateStoryPromptRequest and
TranslateStoryPromptResponse interfaces. extracted_dialogue is string|null
matching the updated BE schema. Existing callers are unaffected
(TypeScript structural typing).
```
