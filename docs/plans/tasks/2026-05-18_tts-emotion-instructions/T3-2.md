# T3-2: 回帰確認 — 既存 dialogue 生成が壊れていないこと

## 依存タスク

- T1-1〜T1-6 完了後
- T2-1〜T2-5 完了後
- T3-1 完了後

## 目的

`tts_instructions` を一切指定しない既存のユーザーフローが壊れていないことを確認する。後方互換 (AC6) を担保する最終チェック。

## 対象ファイル

- 読み取りのみ。変更なし。

## 実装手順

### Step 1: BE 全テスト実行

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
pytest tests/ -v 2>&1 | tail -40
```

既知の失敗 (MEMORY.md 記載): `tests/videos/test_text_to_image.py` ×2、`tests/library/test_service.py` ×1 は本タスクのスコープ外の既存問題。これら以外が pass すること。

### Step 2: FE 全テスト実行

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npm run test -- --run 2>&1 | tail -30
```

全件 pass すること。

### Step 3: TypeScript ビルド確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1
npm run build 2>&1 | tail -20
```

エラーがないことを確認する。

### Step 4: E2E 回帰確認 (オプション、ローカル環境が整っている場合)

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npm run test:e2e 2>&1 | tail -30
```

### Step 5: DB 後方互換確認

Supabase ダッシュボードで `dialogue_generations` テーブルを確認し、既存レコードの `tts_instructions` カラムが NULL であることを確認する。

```sql
SELECT COUNT(*) as total,
       COUNT(tts_instructions) as with_instructions
FROM dialogue_generations
LIMIT 1;
```

`with_instructions = 0` (または既存レコード分は NULL) であれば後方互換確認完了。

## 検証

全ての Step が正常完了すること。

## 完了条件

- `pytest tests/` が既知の失敗 (3 件) 以外すべて pass
- `npm run test -- --run` が全件 pass
- `npx tsc --noEmit` がエラーなし
- `npm run build` が成功
- 既存 `dialogue_generations` レコードの `tts_instructions` カラムが NULL

## コミットメッセージ案

このタスク自体はコードを変更しないため、コミットは不要。回帰確認の結果をドキュメントにメモする程度でよい。
