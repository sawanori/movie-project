# T1-6: BE 単体テスト追加

## 依存タスク

- T1-1 完了後 (provider デフォルト変更)
- T1-2 完了後 (schema フィールド追加)
- T1-3 完了後 (router/service 配線)
- T1-4 完了後 (processor/tts_service/tts_processor 配線)
- T1-5 完了後 (migration 適用済み、または既存 mock ベースで先行可)

## 目的

instructions 関連の各コンポーネントを網羅する単体テストを追加し、AC1 / AC7 / AC10b の検証可能性を担保する。既存テストが T1-4 の引数追加で壊れている場合はあわせて修正する。

## 対象ファイル

- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/dialogue/test_router.py` (既存: 変更なし or 確認のみ)
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/dialogue/test_dialogue_processor.py` (既存: `_make_dialogue_record` に `tts_instructions` 追加 + 新テストケース)
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/tasks/test_tts_processor.py` (既存: `assert_called_once_with` に `instructions=None` 追加)
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external/test_openai_tts_provider.py` (新規作成)

## 実装手順

### Step 1: 既存テスト壊れ箇所を確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
pytest tests/dialogue/ tests/tasks/test_tts_processor.py -v 2>&1 | tail -30
```

T1-4 で `generate_speech` 引数が増えたため `test_tts_processor.py` L50-55 の `assert_called_once_with` が失敗する可能性がある。

### Step 2: test_tts_processor.py の既存テストを修正

`assert_called_once_with` で `instructions=` を追加する。`record.get("instructions")` が None になるため `instructions=None` を期待値に加える:

```python
# 変更前
mock_provider.generate_speech.assert_called_once_with(
    text="こんにちは",
    voice_id="voice_123",
    language="ja",
    speed=1.0,
)

# 変更後
mock_provider.generate_speech.assert_called_once_with(
    text="こんにちは",
    voice_id="voice_123",
    language="ja",
    speed=1.0,
    instructions=None,
)
```

他の `assert_called_once_with` が存在する場合も同様に修正する (grep で確認すること)。

### Step 3: test_dialogue_processor.py の _make_dialogue_record を更新

`_make_dialogue_record` ヘルパー (L26-44) に `tts_instructions=None` を追加:

```python
def _make_dialogue_record(
    generation_id: str | None = None,
    status: str = "processing",
    tts_instructions: str | None = None,  # 新規追加
) -> dict:
    return {
        "id": generation_id or str(uuid.uuid4()),
        "user_id": "user-001",
        ...
        "tts_instructions": tts_instructions,  # 新規追加
    }
```

### Step 4: test_dialogue_processor.py に instructions 新テストケースを追加

既存テストクラスに追加、または新テストクラスとして追加:

```python
class TestRunTtsAndGetAudioUrlWithInstructions:
    """_run_tts_and_get_audio_url が tts_instructions を create_tts_generation に渡す"""

    @pytest.mark.asyncio
    async def test_tts_instructions_propagated_to_create_tts_generation(self):
        """tts_instructions が create_tts_generation に instructions として渡される"""
        from app.tasks.dialogue_processor import _run_tts_and_get_audio_url

        custom_instructions = "Speak with dramatic excitement"

        with patch("app.tasks.dialogue_processor.create_tts_generation", new_callable=AsyncMock) as mock_create_tts, \
             patch("app.tasks.dialogue_processor.process_tts_generation", new_callable=AsyncMock), \
             patch("app.tasks.dialogue_processor.get_tts_status", new_callable=AsyncMock) as mock_get_status, \
             patch("app.tasks.dialogue_processor.update_dialogue_status", new_callable=AsyncMock):

            mock_create_tts.return_value = {"id": "tts-001"}
            mock_get_status.return_value = {
                "id": "tts-001",
                "status": "completed",
                "audio_url": "https://example.com/audio.mp3",
                "duration_seconds": 3.0,
                "error_message": None,
            }

            audio_url = await _run_tts_and_get_audio_url(
                text="こんにちは",
                voice_id="voice_ja",
                language="ja",
                speed=1.0,
                user_id="user-001",
                generation_id="gen-001",
                tts_instructions=custom_instructions,
            )

        # create_tts_generation が instructions 付きで呼ばれたことを確認
        mock_create_tts.assert_called_once()
        call_kwargs = mock_create_tts.call_args.kwargs
        assert call_kwargs.get("instructions") == custom_instructions

    @pytest.mark.asyncio
    async def test_tts_instructions_none_when_not_specified(self):
        """tts_instructions 未指定時は instructions=None が渡される"""
        from app.tasks.dialogue_processor import _run_tts_and_get_audio_url

        with patch("app.tasks.dialogue_processor.create_tts_generation", new_callable=AsyncMock) as mock_create_tts, \
             patch("app.tasks.dialogue_processor.process_tts_generation", new_callable=AsyncMock), \
             patch("app.tasks.dialogue_processor.get_tts_status", new_callable=AsyncMock) as mock_get_status, \
             patch("app.tasks.dialogue_processor.update_dialogue_status", new_callable=AsyncMock):

            mock_create_tts.return_value = {"id": "tts-002"}
            mock_get_status.return_value = {
                "id": "tts-002",
                "status": "completed",
                "audio_url": "https://example.com/audio.mp3",
                "duration_seconds": 3.0,
                "error_message": None,
            }

            await _run_tts_and_get_audio_url(
                text="こんにちは",
                voice_id="voice_ja",
                language="ja",
                speed=1.0,
                user_id="user-001",
                generation_id="gen-002",
                # tts_instructions 未指定
            )

        call_kwargs = mock_create_tts.call_args.kwargs
        assert call_kwargs.get("instructions") is None
```

### Step 5: test_openai_tts_provider.py を新規作成

```
/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external/test_openai_tts_provider.py
```

テスト内容:

```python
"""
OpenAITTSProvider 単体テスト

実際の OpenAI API / R2 は呼び出さない (httpx.AsyncClient と r2_client をモック)
"""
import pytest
from unittest.mock import AsyncMock, MagicMock, patch


class TestOpenAITTSProviderDefaultInstructions:
    """デフォルト instructions 強化のテスト"""

    @pytest.mark.asyncio
    async def test_default_english_instructions_applied_when_instructions_none(self):
        """instructions=None かつ language='ja' で英語デフォルト文が適用される (AC1 / AC10b)"""
        from app.external.openai_tts_provider import OpenAITTSProvider

        provider = OpenAITTSProvider()
        captured_payload = {}

        async def mock_post(url, json, headers):
            captured_payload.update(json)
            mock_response = MagicMock()
            mock_response.raise_for_status = MagicMock()
            mock_response.content = b"fake_audio"
            return mock_response

        with patch("app.external.openai_tts_provider.r2_client") as mock_r2, \
             patch("httpx.AsyncClient") as mock_client_cls:
            mock_r2.upload_file = AsyncMock(return_value="https://r2.example.com/audio.mp3")
            mock_client = AsyncMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=False)
            mock_client.post = AsyncMock(side_effect=mock_post)
            mock_client_cls.return_value = mock_client

            await provider.generate_speech(
                text="こんにちは",
                voice_id="alloy",
                language="ja",
                instructions=None,
            )

        assert "instructions" in captured_payload
        instructions_sent = captured_payload["instructions"]
        assert "Speak natural Japanese" in instructions_sent
        assert "robotic" in instructions_sent  # "avoiding robotic delivery" を含む

    @pytest.mark.asyncio
    async def test_default_instructions_applied_when_instructions_empty_string(self):
        """instructions='' でもデフォルト英語文が適用される (AC10b: 空文字防御)"""
        from app.external.openai_tts_provider import OpenAITTSProvider

        provider = OpenAITTSProvider()
        captured_payload = {}

        async def mock_post(url, json, headers):
            captured_payload.update(json)
            mock_response = MagicMock()
            mock_response.raise_for_status = MagicMock()
            mock_response.content = b"fake_audio"
            return mock_response

        with patch("app.external.openai_tts_provider.r2_client") as mock_r2, \
             patch("httpx.AsyncClient") as mock_client_cls:
            mock_r2.upload_file = AsyncMock(return_value="https://r2.example.com/audio.mp3")
            mock_client = AsyncMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=False)
            mock_client.post = AsyncMock(side_effect=mock_post)
            mock_client_cls.return_value = mock_client

            await provider.generate_speech(
                text="こんにちは",
                voice_id="alloy",
                language="ja",
                instructions="",
            )

        assert "instructions" in captured_payload
        assert "Speak natural Japanese" in captured_payload["instructions"]

    @pytest.mark.asyncio
    async def test_custom_instructions_passed_through(self):
        """instructions='custom' が payload にそのまま含まれる"""
        from app.external.openai_tts_provider import OpenAITTSProvider

        provider = OpenAITTSProvider()
        captured_payload = {}

        async def mock_post(url, json, headers):
            captured_payload.update(json)
            mock_response = MagicMock()
            mock_response.raise_for_status = MagicMock()
            mock_response.content = b"fake_audio"
            return mock_response

        with patch("app.external.openai_tts_provider.r2_client") as mock_r2, \
             patch("httpx.AsyncClient") as mock_client_cls:
            mock_r2.upload_file = AsyncMock(return_value="https://r2.example.com/audio.mp3")
            mock_client = AsyncMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=False)
            mock_client.post = AsyncMock(side_effect=mock_post)
            mock_client_cls.return_value = mock_client

            await provider.generate_speech(
                text="こんにちは",
                voice_id="alloy",
                language="ja",
                instructions="Speak with dramatic excitement",
            )

        assert captured_payload.get("instructions") == "Speak with dramatic excitement"
```

`tests/external/` ディレクトリが存在しない場合は作成し、`__init__.py` を追加する。

```bash
mkdir -p /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external
touch /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/external/__init__.py
```

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api

# 全テスト実行
pytest tests/dialogue/ tests/tasks/test_tts_processor.py tests/external/test_openai_tts_provider.py -v

# カバレッジ確認 (instructions 関連の分岐を網羅)
pytest tests/dialogue/ tests/tasks/test_tts_processor.py tests/external/test_openai_tts_provider.py \
  --cov=app/external/openai_tts_provider \
  --cov=app/tasks/dialogue_processor \
  --cov=app/tasks/tts_processor \
  --cov=app/tts/service \
  --cov-report=term-missing 2>&1 | tail -30
```

## 完了条件

- `pytest tests/dialogue/ tests/tasks/test_tts_processor.py` が全件 pass
- `tests/external/test_openai_tts_provider.py` の 3 テストが pass
- `test_dialogue_processor.py` の instructions 新テスト 2 件が pass
- `generate_speech` の `assert_called_once_with` が `instructions=None` を含んで pass

## コミットメッセージ案

```
test(tts): add unit tests for instructions propagation across BE pipeline

- Fix existing generate_speech assert to include instructions=None arg
- Add _make_dialogue_record tts_instructions field to test helper
- Add tests for _run_tts_and_get_audio_url instructions propagation
- Add tests for OpenAITTSProvider default/empty/custom instructions
```
