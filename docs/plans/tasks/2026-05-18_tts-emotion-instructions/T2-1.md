# T2-1: FE 型定義 + プリセット定数ファイル作成

## 依存タスク

なし (BE タスクと独立。T2 フェーズの最初)

## 目的

フロントエンドの型定義 (`DialogueNodeData`) に `ttsInstructions?: string` を追加し、`createDefaultNodeData('dialogue')` を更新する。あわせて 6 プリセットの定数ファイル `lib/constants/tts-emotion-presets.ts` を新規作成する。

## 対象ファイル

- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/lib/types/node-editor.ts`
  - 変更箇所: `DialogueNodeData` インターフェース (L178-194)、`createDefaultNodeData` 内 `case 'dialogue':` (L479-492)
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/lib/constants/tts-emotion-presets.ts` (新規作成)

## 実装手順

### Step 1: DialogueNodeData に ttsInstructions を追加

`lib/types/node-editor.ts` の `DialogueNodeData` (L178-194):

変更前:
```typescript
export interface DialogueNodeData extends BaseNodeData {
  type: 'dialogue';
  text: string;
  voiceId: string | null;
  language: 'ja';
  speed: number;
  useLipSync: boolean;
  status: 'idle' | 'pending' | 'processing' | 'completed' | 'failed';
  progress: number;
  generationId: string | null;
  outputVideoUrl: string | null;
}
```

変更後 (`useLipSync` の直後に追加):
```typescript
export interface DialogueNodeData extends BaseNodeData {
  type: 'dialogue';
  text: string;
  voiceId: string | null;
  language: 'ja';
  speed: number;
  useLipSync: boolean;
  ttsInstructions?: string;  // 感情/トーン指定。undefined = OpenAI プロバイダーのデフォルト適用
  status: 'idle' | 'pending' | 'processing' | 'completed' | 'failed';
  progress: number;
  generationId: string | null;
  outputVideoUrl: string | null;
}
```

### Step 2: createDefaultNodeData の 'dialogue' ケースを更新

`createDefaultNodeData` 内の `case 'dialogue':` (L479-492):

変更前:
```typescript
case 'dialogue':
  return {
    type: 'dialogue',
    isValid: false,
    text: '',
    voiceId: null,
    language: 'ja',
    speed: 1.0,
    useLipSync: false,
    status: 'idle',
    progress: 0,
    generationId: null,
    outputVideoUrl: null,
  };
```

変更後:
```typescript
case 'dialogue':
  return {
    type: 'dialogue',
    isValid: false,
    text: '',
    voiceId: null,
    language: 'ja',
    speed: 1.0,
    useLipSync: false,
    ttsInstructions: undefined,  // undefined = デフォルト適用。空文字ではなく undefined にすること (AC10a)
    status: 'idle',
    progress: 0,
    generationId: null,
    outputVideoUrl: null,
  };
```

### Step 3: tts-emotion-presets.ts を新規作成

```typescript
/**
 * TTS 感情プリセット定義
 *
 * gpt-4o-mini-tts の instructions パラメータで使用する 6 プリセット。
 * instructions は英語固定 (英語の方が gpt-4o-mini-tts の解釈精度が高い)。
 */

export interface TTSEmotionPreset {
  key: 'joy' | 'sadness' | 'anger' | 'surprise' | 'calm' | 'confusion';
  emoji: string;
  labelJa: string;
  instructions: string;
}

export const TTS_EMOTION_PRESETS: TTSEmotionPreset[] = [
  {
    key: 'joy',
    emoji: '😊',
    labelJa: '喜び',
    instructions:
      'Speak with bright, cheerful enthusiasm. Use rising intonation and convey happiness and warmth.',
  },
  {
    key: 'sadness',
    emoji: '😢',
    labelJa: '悲しみ',
    instructions:
      'Speak softly with a slower pace and downward intonation. Convey sadness and melancholy without being melodramatic.',
  },
  {
    key: 'anger',
    emoji: '😠',
    labelJa: '怒り',
    instructions:
      'Speak with firm, sharp delivery and emphatic pauses. Convey controlled anger and frustration.',
  },
  {
    key: 'surprise',
    emoji: '😲',
    labelJa: '驚き',
    instructions:
      'Speak with sudden rising intonation and emphatic stress. Convey genuine surprise and astonishment.',
  },
  {
    key: 'calm',
    emoji: '😌',
    labelJa: '落ち着き',
    instructions:
      'Speak with calm, even tone and steady pace. Convey serene composure and gentle reassurance.',
  },
  {
    key: 'confusion',
    emoji: '😕',
    labelJa: '困惑',
    instructions:
      'Speak with hesitant pauses and uncertain intonation. Convey confusion and bewilderment, as if thinking aloud.',
  },
];
```

### Step 4: 変更後を確認

`node-editor.ts` の `DialogueNodeData` と `createDefaultNodeData` を読んで正しく変更されていることを確認する。`tts-emotion-presets.ts` が 6 件のプリセットを含んでいることを確認する。

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker

# TypeScript 型チェック
npx tsc --noEmit 2>&1 | head -30

# 既存テスト (Vitest) が pass すること
npm run test -- --run 2>&1 | tail -20
```

## 完了条件

- `DialogueNodeData` に `ttsInstructions?: string` が追加されていること
- `createDefaultNodeData('dialogue')` が `ttsInstructions: undefined` を含んでいること
- `lib/constants/tts-emotion-presets.ts` が 6 プリセットを含んで作成されていること
- `npx tsc --noEmit` がエラーなし
- `npm run test` が全件 pass

## コミットメッセージ案

```
feat(types): add ttsInstructions to DialogueNodeData and create emotion presets

Add optional ttsInstructions field to DialogueNodeData and initialize
to undefined in createDefaultNodeData. Create TTS_EMOTION_PRESETS
constant with 6 English instructions for joy/sadness/anger/surprise/
calm/confusion.
```
