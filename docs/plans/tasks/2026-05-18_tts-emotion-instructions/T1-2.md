# T1-2: DialogueCreateRequest に tts_instructions フィールドを追加

## 依存タスク

なし (T1-1 / T1-5 と並列実行可能)

## 目的

`app/dialogue/schemas.py` の `DialogueCreateRequest` に `tts_instructions: Optional[str]` フィールドを追加し、POST `/api/v1/dialogue` で UI からの instructions を受け取れるようにする。

## 対象ファイル

- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/dialogue/schemas.py`
  - 変更箇所: `DialogueCreateRequest` クラス (L12-25)

## 実装手順

### Step 1: ファイルを読む

`schemas.py` 全体を確認する (L1-41 既読)。現在 `DialogueCreateRequest` は L12-25 に 5 フィールドのみ。

### Step 2: tts_instructions フィールドを追加

`use_lip_sync` フィールドの直後に追記する:

```python
tts_instructions: Optional[str] = Field(
    default=None,
    max_length=1000,
    description=(
        "感情/トーン指定 (gpt-4o-mini-tts のみ適用)。"
        "英語推奨。未指定の場合は OpenAI プロバイダーのデフォルト instructions が適用される。"
        "ElevenLabs プロバイダーでは無視される。"
    ),
)
```

完成後のクラスは:

```python
class DialogueCreateRequest(BaseModel):
    """Dialogue 生成リクエスト"""

    video_url: str = Field(..., description="入力動画の公開 URL (R2 等)")
    text: str = Field(..., min_length=1, max_length=5000, description="セリフテキスト")
    voice_id: str = Field(..., description="TTS 音声 ID")
    language: str = Field(default="ja", description="言語コード (固定: ja)")
    speed: float = Field(default=1.0, ge=0.25, le=4.0, description="読み上げ速度")
    use_lip_sync: bool = Field(
        default=False,
        description="True の場合 Hedra でリップシンクを行う。False は ffmpeg 単純ミックス",
    )
    tts_instructions: Optional[str] = Field(
        default=None,
        max_length=1000,
        description=(
            "感情/トーン指定 (gpt-4o-mini-tts のみ適用)。"
            "英語推奨。未指定の場合は OpenAI プロバイダーのデフォルト instructions が適用される。"
            "ElevenLabs プロバイダーでは無視される。"
        ),
    )
```

### Step 3: 変更後を確認

変更後のファイルを読んで `Optional[str]`、`max_length=1000`、`default=None` が正しく設定されていることを確認する。

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api

# Pydantic バリデーション動作確認 (python インタープリタで簡易チェック)
python3 -c "
from app.dialogue.schemas import DialogueCreateRequest
# 1. tts_instructions 未指定 → None
req = DialogueCreateRequest(video_url='http://x.com/v.mp4', text='hello', voice_id='v1')
assert req.tts_instructions is None, 'should be None'

# 2. 通常文字列
req2 = DialogueCreateRequest(video_url='http://x.com/v.mp4', text='hello', voice_id='v1', tts_instructions='Speak softly')
assert req2.tts_instructions == 'Speak softly'

# 3. 1000 文字は OK
req3 = DialogueCreateRequest(video_url='http://x.com/v.mp4', text='hello', voice_id='v1', tts_instructions='a' * 1000)
assert len(req3.tts_instructions) == 1000

# 4. 1001 文字は ValidationError
from pydantic import ValidationError
try:
    DialogueCreateRequest(video_url='http://x.com/v.mp4', text='hello', voice_id='v1', tts_instructions='a' * 1001)
    assert False, 'should have raised'
except ValidationError:
    pass

print('ALL PASSED')
"

# 既存テスト (router) が pass すること
pytest tests/dialogue/test_router.py -v
```

## 完了条件

- `DialogueCreateRequest` に `tts_instructions: Optional[str] = Field(default=None, max_length=1000, ...)` が追加されていること
- 1001 文字で `ValidationError` が発生すること
- `pytest tests/dialogue/test_router.py` が全件 pass (新フィールドは Optional なので既存テスト無変更で通過するはず)

## コミットメッセージ案

```
feat(dialogue): add tts_instructions field to DialogueCreateRequest schema

Add optional tts_instructions field (max 1000 chars) to accept emotion/
tone instructions for gpt-4o-mini-tts. Follows existing Field() pattern.
```
