# T3-1: 統合テスト + E2E テスト追加

## 依存タスク

- T1-1〜T1-6 完了後 (BE パイプライン全段完成)
- T2-1〜T2-5 完了後 (FE 全実装完成)

## 目的

FE → API → BE → DB → TTS プロバイダーの貫通を検証する統合テスト (pytest) と E2E テスト (Playwright) を追加する。AC1〜AC10b を自動テストで担保する。

## 対象ファイル

- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/dialogue/test_router.py` (既存: instructions 付きリクエストのテストを追加)
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/tests/` または `e2e/` ディレクトリ内 (Playwright E2E)

## 実装手順

### Step 1: 既存のテスト構造を確認

```bash
# BE: 既存 router テストの構造を確認
cat -n /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/tests/dialogue/test_router.py | head -80

# FE: E2E テストディレクトリを確認
ls /Users/noritakasawada/AI_P/practice/movie-project/movie-maker/e2e/ 2>/dev/null || \
  ls /Users/noritakasawada/AI_P/practice/movie-project/movie-maker/tests/ 2>/dev/null | head -20
```

### Step 2: BE 統合テスト追加 (test_router.py)

既存 `TestCreateDialogue` クラスに以下を追加する:

```python
def test_create_dialogue_with_tts_instructions(self, auth_client):
    """POST /dialogue に tts_instructions を含めて呼ぶと正常に起動する"""
    record = _make_record()
    record["tts_instructions"] = "Speak with dramatic excitement"

    with patch(
        "app.dialogue.router.create_dialogue_generation",
        new_callable=AsyncMock,
        return_value=record,
    ) as mock_create, patch(
        "app.dialogue.router.start_dialogue_processing",
        new_callable=AsyncMock,
    ):
        resp = auth_client.post(
            "/api/v1/dialogue",
            json={
                "video_url": "https://example.com/video.mp4",
                "text": "こんにちは",
                "voice_id": "voice_ja_001",
                "tts_instructions": "Speak with dramatic excitement",
            },
        )

    assert resp.status_code == 200
    # create_dialogue_generation が tts_instructions を受け取ったことを確認
    call_kwargs = mock_create.call_args.kwargs
    assert call_kwargs.get("tts_instructions") == "Speak with dramatic excitement"

def test_create_dialogue_instructions_max_length_validation(self, auth_client):
    """tts_instructions が 1001 文字以上の場合 422 が返る (AC9)"""
    with patch("app.dialogue.router.start_dialogue_processing", new_callable=AsyncMock):
        resp = auth_client.post(
            "/api/v1/dialogue",
            json={
                "video_url": "https://example.com/video.mp4",
                "text": "こんにちは",
                "voice_id": "voice_ja_001",
                "tts_instructions": "a" * 1001,
            },
        )

    assert resp.status_code == 422

def test_create_dialogue_without_instructions_uses_default(self, auth_client):
    """tts_instructions 未指定の場合は後方互換 (AC6)"""
    record = _make_record()

    with patch(
        "app.dialogue.router.create_dialogue_generation",
        new_callable=AsyncMock,
        return_value=record,
    ) as mock_create, patch(
        "app.dialogue.router.start_dialogue_processing",
        new_callable=AsyncMock,
    ):
        resp = auth_client.post(
            "/api/v1/dialogue",
            json={
                "video_url": "https://example.com/video.mp4",
                "text": "こんにちは",
                "voice_id": "voice_ja_001",
            },
        )

    assert resp.status_code == 200
    call_kwargs = mock_create.call_args.kwargs
    assert call_kwargs.get("tts_instructions") is None
```

### Step 3: Playwright E2E テスト追加

Playwright の既存 E2E ディレクトリを確認し、該当ディレクトリに `dialogue-emotion.spec.ts` を作成する:

```bash
# E2E テストの場所を確認
find /Users/noritakasawada/AI_P/practice/movie-project/movie-maker -name "*.spec.ts" | grep -v node_modules | head -10
```

E2E テスト (シナリオのみ定義、実装は既存パターンに合わせる):

```typescript
// dialogue-emotion.spec.ts
import { test, expect } from '@playwright/test';

test.describe('DialogueNode 感情パネル', () => {
  test.beforeEach(async ({ page }) => {
    // ログイン済みセッションを設定 (既存 E2E パターンに合わせる)
    await page.goto('/generate');
  });

  test('プリセット選択後に合成できる (AC2)', async ({ page }) => {
    // 1. DialogueNode を配置
    // 2. 感情パネルを展開
    // 3. 「喜び」プリセットを選択
    // 4. textarea に期待値が入っていることを確認
    // 5. 合成ボタンを押す
    // (実際のAPI呼び出しはモックまたはテスト環境の設定に依存)
  });

  test('何も指定せず合成できる (AC6: 後方互換)', async ({ page }) => {
    // 1. DialogueNode を配置
    // 2. 感情パネルを開かずに合成
    // 3. エラーが出ないことを確認
  });
});
```

注意: E2E テストは実際の Supabase/API との接続が必要なため、ローカル環境での動作確認と合わせて実装すること。playwright.config.ts の `NEXT_PUBLIC_E2E_TEST_MODE` 設定を確認する。

## 検証

```bash
# BE 統合テスト
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
pytest tests/dialogue/test_router.py -v -k "instructions"

# BE 全テスト
pytest tests/dialogue/ tests/tasks/ tests/external/ -v 2>&1 | tail -30

# FE E2E (ローカル環境が必要)
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npm run test:e2e -- --grep "感情パネル" 2>&1 | tail -30
```

## 完了条件

- BE: `test_create_dialogue_with_tts_instructions` が pass (AC2 への準拠)
- BE: `test_create_dialogue_instructions_max_length_validation` が pass (AC9)
- BE: `test_create_dialogue_without_instructions_uses_default` が pass (AC6)
- `pytest tests/dialogue/test_router.py` が全件 pass
- E2E テストファイルが作成されていること (実行は T3-2 で確認)

## コミットメッセージ案

```
test(integration): add dialogue API tests for tts_instructions handling

Verify instructions propagation through router (AC2, AC6), max length
validation (AC9, 422 response), and backward compatibility with missing
instructions field. Add Playwright E2E scenarios for emotion preset flow.
```
