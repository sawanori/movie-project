# T4-1: 品質ゲート — tsc / lint / build / pytest 全パス確認

## 依存タスク

- T1-1〜T1-6、T2-1〜T2-5、T3-1〜T3-2 全完了後

## 目的

全実装完了後の最終品質ゲートチェック。静的解析、ビルド、テストが全て pass することを確認し、本機能がリリース可能状態であることを担保する。

## 対象ファイル

- 読み取りのみ。変更が必要な場合は各担当タスクに戻って修正する。

## 実装手順

### Phase 1: 静的解析

#### BE: Python lint

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api

# ruff または flake8 (プロジェクトの設定に従う)
ruff check app/external/openai_tts_provider.py \
           app/dialogue/schemas.py \
           app/dialogue/router.py \
           app/dialogue/service.py \
           app/tasks/dialogue_processor.py \
           app/tts/service.py \
           app/tasks/tts_processor.py 2>&1

# または
python3 -m flake8 app/ --max-line-length=120 --exclude=__pycache__ 2>&1 | head -30
```

#### FE: ESLint

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npm run lint 2>&1 | head -40
```

### Phase 2: 型チェック

#### FE: TypeScript

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit 2>&1
```

エラーなしで完了すること。

### Phase 3: ビルド

#### FE: Next.js production build

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npm run build 2>&1 | tail -20
```

ビルドが成功すること。

### Phase 4: テスト

#### BE: pytest

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
pytest tests/ -v 2>&1 | tail -40
```

期待: 既知の失敗 (`tests/videos/test_text_to_image.py` ×2、`tests/library/test_service.py` ×1) 以外は全件 pass。本機能追加分のテスト (T1-6 / T3-1 で追加) が全件 pass すること。

#### FE: Vitest

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npm run test -- --run 2>&1 | tail -30
```

全件 pass すること。

### Phase 5: 最終確認チェックリスト

以下を全て確認する:

- [ ] T1-1: `openai_tts_provider.py` のデフォルト instructions が英語 4 文になっている
- [ ] T1-2: `DialogueCreateRequest` に `tts_instructions: Optional[str]` (max_length=1000) が追加されている
- [ ] T1-3: `create_dialogue_generation` が `tts_instructions` を DB に INSERT している
- [ ] T1-4: `dialogue_processor` → `tts_processor` → `generate_speech` に instructions が貫通している
- [ ] T1-5: `dialogue_generations.tts_instructions` と `tts_generations.instructions` カラムが Supabase に存在する
- [ ] T1-6: BE 新規テストが全件 pass している
- [ ] T2-1: `DialogueNodeData.ttsInstructions?: string` が型定義に存在する
- [ ] T2-1: `TTS_EMOTION_PRESETS` が 6 件定義されている
- [ ] T2-2: `DialogueCreatePayload.tts_instructions?: string` が追加されている
- [ ] T2-3: DialogueNode に折りたたみパネル (プリセット + textarea + 注記) が実装されている
- [ ] T2-4: `handleStartDialogue` で `tts_instructions: data.ttsInstructions || undefined` を渡している
- [ ] T2-5: FE 単体テストが全件 pass している
- [ ] T3-1: 統合テスト (BE router) が pass している
- [ ] T3-2: 既存テストが回帰なしで pass している

## 検証コマンド (まとめ)

```bash
# BE
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
pytest tests/ -v 2>&1 | grep -E "PASSED|FAILED|ERROR" | tail -30

# FE
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker
npx tsc --noEmit && npm run lint && npm run build && npm run test -- --run
```

## 完了条件

- BE: `pytest tests/` で既知の 3 件以外は全件 pass
- FE: `npx tsc --noEmit` がエラーなし
- FE: `npm run lint` がエラーなし
- FE: `npm run build` が成功
- FE: `npm run test -- --run` が全件 pass
- 最終確認チェックリストの全項目に ✓ がつく

## コミットメッセージ案

このタスク自体はコードを変更しないため、コミットは不要。品質ゲート通過を確認した後、本機能ブランチの PR を作成する。
