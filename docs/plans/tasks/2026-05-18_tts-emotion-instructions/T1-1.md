# T1-1: OpenAITTSProvider デフォルト instructions を英語化

## 依存タスク

なし (独立して実行可能)

## 目的

現在 `generate_speech` の `instructions is None and language == "ja"` 判定で適用される日本語デフォルト文を新しい英語版に差し替える。あわせて判定を `if (not instructions)` に変更し、空文字・whitespace-only も「未指定」として同じデフォルトを適用する (AC1 / AC10b)。

## 対象ファイル

- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api/app/external/openai_tts_provider.py`
  - 変更箇所: L79-83 (デフォルト instructions 適用ブロック)

## 実装手順

### Step 1: ファイルを読む

`openai_tts_provider.py` L79-83 を確認する (既読)。現在の実装:

```python
if instructions is None and language == "ja":
    instructions = (
        "自然で聞き取りやすい日本語で読み上げてください。"
        "テキストの感情に合わせた抑揚と表現を心がけてください。"
    )
```

### Step 2: 変更を適用

L79 の条件式と L80-83 の文字列を以下に置き換える:

```python
# language="ja" かつ instructions 未指定 (None / 空文字 / whitespace-only) の場合は
# デフォルト英語 instructions を適用する (AC10b: 空文字も「未指定」扱い)
if (not instructions) and language == "ja":
    instructions = (
        "Speak natural Japanese with rich emotional expression. "
        "Vary pitch, pace, and emphasis to convey the underlying feelings in the text. "
        "Use human-like pauses and intonation, avoiding robotic delivery. "
        "Match the tone to the dialogue's mood (joy, sadness, anger, surprise, etc.) as appropriate."
    )
```

### Step 3: 変更箇所の確認

変更後の L78-93 を読んで、`if (not instructions)` が正しく入り、文字列が 4 文で構成されていることを確認する。

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker-api
# openai_tts_provider に関するテストのみ実行 (既存テストが壊れていないことを確認)
pytest tests/ -k "tts" -v 2>&1 | head -60
```

既存テストが pass すること。このタスク単体では新テストを追加しない (T1-6 で追加)。

## 完了条件

- `if (not instructions) and language == "ja":` に変更されていること
- デフォルト文が英語 4 文 (Speak natural / Vary pitch / Use human-like / Match the tone) になっていること
- `pytest tests/ -k "tts"` が全件 pass

## コミットメッセージ案

```
feat(tts): strengthen default instructions to English for gpt-4o-mini-tts

Replace Japanese default instructions with English equivalent that more
precisely guides gpt-4o-mini-tts on pitch, pace, emphasis and tone.
Also change guard condition from `is None` to `not` to treat empty
strings as unspecified (AC10b).
```
