# T1-5: isValidConnection 実装 (NodeEditor.tsx)

依存タスク: T1-1 完了後、T1-2 完了後

## 目的

`NodeEditor.tsx` の `<ReactFlow>` に `isValidConnection` prop を追加し、ProviderNode の各 target Handle に対して互換性のない source ノードタイプからの接続をドラッグ時点でブロックする。これにより AC-2 を達成する。

## 対象ファイル

編集:
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/NodeEditor.tsx`
  - L975-994: `<ReactFlow>` の props に `isValidConnection` を追加
  - L240 周辺: `useCallback` と `HANDLE_IDS` のインポートを確認・追加

## 実装手順

### Step 1: NodeEditor.tsx を読み込む

`/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/NodeEditor.tsx` の L1-50 (imports) と L975-1000 (`<ReactFlow>` props) を Read する。

### Step 2: imports を確認・追加

ファイル冒頭の imports を確認し、必要に応じて以下を追加:

```typescript
import type { Connection } from '@xyflow/react';
import { HANDLE_IDS } from '@/lib/types/node-editor';
import type { WorkflowNodeData } from '@/lib/types/node-editor';
```

`useCallback` は既に react からインポートされているはずだが確認すること。

### Step 3: isValidConnection コールバックを定義

`useWorkflowValidation` の呼び出し (L240) の後に追加:

```typescript
/**
 * ProviderNode の各 target Handle に対し、互換性のある source ノードタイプのみ接続を許可する。
 * それ以外の既存 Handle (CONFIG_INPUT 等) はこのガードの対象外。
 */
const isValidConnection = useCallback(
  (connection: Connection): boolean => {
    // ProviderNode の各入力 Handle と受け付ける source ノード type のマッピング
    const HANDLE_TO_NODE_TYPE: Partial<Record<string, WorkflowNodeData['type']>> = {
      [HANDLE_IDS.KLING_MODE_INPUT]: 'klingMode',
      [HANDLE_IDS.KLING_ELEMENTS_INPUT]: 'klingElements',
      [HANDLE_IDS.KLING_END_FRAME_INPUT]: 'klingEndFrame',
      [HANDLE_IDS.KLING_CAMERA_CONTROL_INPUT]: 'klingCameraControl',
      [HANDLE_IDS.ACT_TWO_INPUT]: 'actTwo',
      [HANDLE_IDS.HAILUO_END_FRAME_INPUT]: 'hailuoEndFrame',
    };

    const expectedType = HANDLE_TO_NODE_TYPE[connection.targetHandle ?? ''];
    // 対象外の Handle (CONFIG_INPUT 等) は常に true
    if (!expectedType) return true;

    const sourceNode = nodes.find((n) => n.id === connection.source);
    if (!sourceNode) return false;
    return (sourceNode.data as WorkflowNodeData).type === expectedType;
  },
  [nodes]
);
```

注意: `nodes` は NodeEditor コンポーネントの state であり、`useCallback` の依存配列に含める。

### Step 4: `<ReactFlow>` に prop を追加

L975-994 の `<ReactFlow>` に `isValidConnection` を追加:

```tsx
<ReactFlow
  nodes={nodes}
  edges={edges}
  onNodesChange={onNodesChange}
  onEdgesChange={onEdgesChange}
  onConnect={onConnect}
  onDrop={onDrop}
  onDragOver={onDragOver}
  nodeTypes={nodeTypes}
  defaultEdgeOptions={defaultEdgeOptions}
  connectionLineStyle={connectionLineStyle}
  isValidConnection={isValidConnection}   // 追加
  fitView
  ...
>
```

### Step 5: 型チェック確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

### Step 6: 動作確認 (目視)

ローカルで `npm run dev` 起動後:
1. KlingModeNode と ProviderNode をキャンバスに配置
2. KlingModeNode のソースハンドルを ProviderNode の `KLING_MODE_INPUT` に接続 → 成功すること
3. ActTwoNode のソースハンドルを ProviderNode の `KLING_MODE_INPUT` に接続しようとする → 接続されないこと (線がはじかれる)

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

エラー 0 件。

## 完了条件 (Acceptance Criteria)

- AC-2: ActTwoNode のソース (`act_two`) を ProviderNode の `KLING_ELEMENTS_INPUT` にドラッグしても接続されない
- `isValidConnection` が `<ReactFlow>` の prop として設定されている
- ProviderNode の Handle ID に一致する source ノードタイプのみ接続を許可するロジックが実装されている
- 既存の接続 (CONFIG_INPUT, IMAGE_INPUT 等) が影響を受けないこと
- `npx tsc --noEmit` エラーなし

## コミットメッセージ案

```
feat(node-editor): add isValidConnection guard to block incompatible handle connections
```
