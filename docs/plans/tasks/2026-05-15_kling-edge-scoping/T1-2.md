# T1-2: ProviderNode に target Handle 6 個追加 + KlingCameraControlNode の未使用 target 削除

依存タスク: T1-1 完了後

## 目的

`ProviderNode.tsx` の左側に 6 個の target Handle を追加し、Kling/Runway/Hailuo 設定ノードをエッジで接続可能にする。ノード最小高さ 240px を確保し、互換性のない Handle を `opacity-30` でグレーアウトする。合わせて `KlingCameraControlNode.tsx` の未使用 `PROVIDER_INPUT` target (§11-5=A 確定) を削除して新規 source-only 構造に整理する。

## 対象ファイル

編集:
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/ProviderNode.tsx`
  - L155-161: 既存 source Handle の直前に 6 個の target Handle を追加
  - ノード本体の `BaseNode` に `style={{ minHeight: '240px' }}` を追加
  - プロバイダーに応じた `opacity-30` グレーアウトロジックを追加
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/KlingCameraControlNode.tsx`
  - L73-78: `type="target"` の `provider` Handle を削除 (§11-5=A)

## 実装手順

### Step 1: ProviderNode.tsx を読み込む

`/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/ProviderNode.tsx` 全体を Read する。

### Step 2: imports に HANDLE_IDS を追加

ファイル冒頭の import 群に追加:

```typescript
import { HANDLE_IDS } from '@/lib/types/node-editor';
```

### Step 3: プロバイダー互換性マップを定数として定義

コンポーネント定義の前 (DURATION_OPTIONS の後) に追加:

```typescript
// Handle ごとに対応するプロバイダーを定義
const HANDLE_PROVIDER_COMPAT: Record<string, VideoProvider[]> = {
  [HANDLE_IDS.KLING_MODE_INPUT]: ['piapi_kling'],
  [HANDLE_IDS.KLING_ELEMENTS_INPUT]: ['piapi_kling'],
  [HANDLE_IDS.KLING_END_FRAME_INPUT]: ['piapi_kling'],
  [HANDLE_IDS.KLING_CAMERA_CONTROL_INPUT]: ['piapi_kling'],
  [HANDLE_IDS.ACT_TWO_INPUT]: ['runway'],
  [HANDLE_IDS.HAILUO_END_FRAME_INPUT]: ['hailuo'],
};
```

### Step 4: target Handle 6 個を JSX に追加

既存の `{/* 出力ハンドル */}` ブロックの直前に以下を追加する。`style={{ top: '%' }}` で縦位置を制御する:

```tsx
{/* 入力ハンドル (Kling/Runway/Hailuo 設定ノード接続) */}
{[
  { id: HANDLE_IDS.KLING_MODE_INPUT, label: 'Kling Mode', top: '10%' },
  { id: HANDLE_IDS.KLING_ELEMENTS_INPUT, label: 'Elements', top: '25%' },
  { id: HANDLE_IDS.KLING_END_FRAME_INPUT, label: 'End Frame', top: '40%' },
  { id: HANDLE_IDS.KLING_CAMERA_CONTROL_INPUT, label: 'Camera', top: '55%' },
  { id: HANDLE_IDS.ACT_TWO_INPUT, label: 'Act-Two', top: '70%' },
  { id: HANDLE_IDS.HAILUO_END_FRAME_INPUT, label: 'Hailuo EF', top: '85%' },
].map(({ id, label, top }) => {
  const isCompat = HANDLE_PROVIDER_COMPAT[id]?.includes(data.provider) ?? true;
  return (
    <Handle
      key={id}
      type="target"
      position={Position.Left}
      id={id}
      className={cn(
        inputHandleClassName,
        !isCompat && 'opacity-30 cursor-not-allowed'
      )}
      style={{ top }}
      title={
        !isCompat
          ? `このプロバイダーでは使用されません (${label})`
          : label
      }
    />
  );
})}
```

注意: `inputHandleClassName` が `ProviderNode.tsx` の imports に含まれているか確認し、なければ `BaseNode.tsx` からインポートを追加する。

### Step 5: BaseNode の最小高さを確保

`<BaseNode>` に `style` prop を追加:

```tsx
<BaseNode
  ...
  style={{ minHeight: '240px' }}
>
```

### Step 6: KlingCameraControlNode.tsx の未使用 target を削除

`/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/nodes/KlingCameraControlNode.tsx` L73-78 の以下ブロックを削除する:

```tsx
{/* Provider入力ハンドル */}
<Handle
  type="target"
  position={Position.Left}
  id="provider"
  className={inputHandleClassName}
/>
```

### Step 7: 型チェック + ビルド確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

### Step 8: 目視確認

`npm run dev` でローカル起動し、NodeEditor でProviderNode をキャンバスに配置し、左側に 6 個の Handle が縦に表示されることを確認する。プロバイダーを切替えると互換性のない Handle が薄く (opacity-30) なることも確認する。

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

エラー 0 件であること。

## 完了条件 (Acceptance Criteria)

- AC-1: ProviderNode の左側に 6 個の target Handle が `data-handleid` 属性付きで表示される
- 各 Handle の縦位置が 10/25/40/55/70/85% であること
- `data.provider === 'piapi_kling'` 時に `ACT_TWO_INPUT`・`HAILUO_END_FRAME_INPUT` が `opacity-30` になること
- `data.provider === 'runway'` 時に `KLING_*` 4 Handle が `opacity-30` になること
- `data.provider === 'hailuo'` 時に `KLING_*` 4 Handle と `ACT_TWO_INPUT` が `opacity-30` になること
- ProviderNode の最小高さが 240px であること
- KlingCameraControlNode に `type="target" id="provider"` の Handle が存在しないこと
- `npx tsc --noEmit` がエラーなし

## コミットメッセージ案

```
feat(node-editor): add 6 target handles to ProviderNode and remove unused CameraControl provider target
```
