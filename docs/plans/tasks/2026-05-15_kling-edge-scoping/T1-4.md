# T1-4: バリデーション拡張 (warnings + ambiguous_provider + disconnected_optional + provider_mismatch 強化)

依存タスク: T1-1 完了後、T1-3 完了後

## 目的

`useWorkflowValidation.ts` に `warnings: ValidationError[]` 配列を新規追加し、`disconnected_optional`・`ambiguous_provider`・`multiple_provider_connection` の 3 種の新しいチェックを実装する。`NodeEditor.tsx` で `warnings` をトーストで表示する出力を追加する。

## 対象ファイル

編集:
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/hooks/useWorkflowValidation.ts`
  - L14-18: `ValidationResult` インターフェースに `warnings` を追加
  - L27-171: `useMemo` ブロック内に新しいチェックを追加
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/NodeEditor.tsx`
  - L240: `useWorkflowValidation` の destructuring に `warnings` を追加
  - warnings を表示する UI (トーストまたはバナー) を追加

## 実装手順

### Step 1: useWorkflowValidation.ts を読み込む

`/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/components/node-editor/hooks/useWorkflowValidation.ts` 全体を Read する。

### Step 2: ValidationResult に warnings を追加

`ValidationResult` インターフェース:

```typescript
interface ValidationResult {
  errors: ValidationError[];
  warnings: ValidationError[];  // 新規追加
  isValid: boolean;
  canGenerate: boolean;
}
```

### Step 3: useMemo 内の型・変数を追加

`const errors: ValidationError[] = [];` の直下に追加:

```typescript
const warnings: ValidationError[] = [];
```

### Step 4: 追加インポート

```typescript
import type { ..., GenerateNodeData, KlingModeNodeData, KlingEndFrameNodeData, KlingCameraControlNodeData, HailuoEndFrameNodeData } from '@/lib/types/node-editor';
import type { Edge } from '@xyflow/react';
import { HANDLE_IDS } from '@/lib/types/node-editor';
```

既存のインポートに不足しているものがあれば追加する。

### Step 5: エッジトレース用ヘルパーを追加 (useMemo 内)

既存の `findNode` の直下に追加:

```typescript
// エッジから接続元ノードのデータを取得するヘルパー
const findConnectedSource = <T extends WorkflowNodeData>(
  targetNodeId: string,
  targetHandleId: string
): T | undefined => {
  const edge = edges.find(
    (e) => e.target === targetNodeId && e.targetHandle === targetHandleId
  );
  if (!edge) return undefined;
  const sourceNode = nodes.find((n) => n.id === edge.source);
  return sourceNode?.data as T | undefined;
};
```

### Step 6: 既存の provider_mismatch チェックをエッジベースに強化

既存の `selectedProvider !== 'piapi_kling'` チェック (L122-138) の直後に、エッジ接続された場合の provider_mismatch チェックを追加する。

ActTwoNode が piapi_kling ProviderNode に接続されている場合:
```typescript
// エッジ接続された場合の不整合チェック
const allGenerateNodes = nodes.filter(
  (n) => (n.data as WorkflowNodeData).type === 'generate'
);
for (const genNode of allGenerateNodes) {
  const connectedProvider = findConnectedSource<ProviderNodeData>(
    genNode.id,
    HANDLE_IDS.CONFIG_INPUT
  );
  if (!connectedProvider) continue;

  // ActTwoNode が Kling Provider に接続
  const actTwoViaEdge = findConnectedSource<ActTwoNodeData>(
    connectedProvider.nodeId ?? '',  // ProviderNode 自身の ID を取得するには nodes から引く
    HANDLE_IDS.ACT_TWO_INPUT
  );
  // 注: findConnectedSource は targetNodeId を使うので、ProviderNode の ID が必要
  // 以下に示す実装パターンを参照
}
```

実装パターン (上記の簡略化版):

```typescript
// 全 GenerateNode → ProviderNode → 設定ノードのエッジを検査
for (const genNode of nodes.filter(n => (n.data as WorkflowNodeData).type === 'generate')) {
  const configEdge = edges.find(
    e => e.target === genNode.id && e.targetHandle === HANDLE_IDS.CONFIG_INPUT
  );
  if (!configEdge) continue;
  const provNode = nodes.find(n => n.id === configEdge.source);
  if (!provNode) continue;
  const provData = provNode.data as ProviderNodeData;

  // ActTwoNode が非 runway Provider に接続
  const actTwoEdge = edges.find(
    e => e.target === provNode.id && e.targetHandle === HANDLE_IDS.ACT_TWO_INPUT
  );
  if (actTwoEdge && provData.provider !== 'runway') {
    const actTwoNode = nodes.find(n => n.id === actTwoEdge.source);
    if (actTwoNode) {
      errors.push({
        type: 'provider_mismatch',
        nodeId: actTwoNode.id,
        message: 'Act-TwoはRunwayプロバイダー専用です (エッジ接続不整合)',
      });
    }
  }

  // KlingElementsNode が非 piapi_kling Provider に接続
  const klingElemEdge = edges.find(
    e => e.target === provNode.id && e.targetHandle === HANDLE_IDS.KLING_ELEMENTS_INPUT
  );
  if (klingElemEdge && provData.provider !== 'piapi_kling') {
    const klingElemNode = nodes.find(n => n.id === klingElemEdge.source);
    if (klingElemNode) {
      errors.push({
        type: 'provider_mismatch',
        nodeId: klingElemNode.id,
        message: 'Kling要素画像ノードはKlingプロバイダー専用です (エッジ接続不整合)',
      });
    }
  }

  // CONFIG_INPUT に複数 Provider 接続チェック
  const multiProvEdges = edges.filter(
    e => e.target === genNode.id && e.targetHandle === HANDLE_IDS.CONFIG_INPUT
  );
  if (multiProvEdges.length > 1) {
    errors.push({
      type: 'multiple_provider_connection',
      nodeId: genNode.id,
      message: 'GenerateNode の CONFIG_INPUT に複数の ProviderNode が接続されています',
    });
  }
}
```

### Step 7: disconnected_optional warning を追加

全 Kling 系設定ノードについて、どの ProviderNode にも接続されていないノードを検出:

```typescript
// 設定ノードが ProviderNode に未接続の場合 warning
const providerSpecificTypes = [
  'klingMode', 'klingElements', 'klingEndFrame',
  'klingCameraControl', 'actTwo', 'hailuoEndFrame'
] as const;

for (const nodeType of providerSpecificTypes) {
  const specNode = nodes.find(n => (n.data as WorkflowNodeData).type === nodeType);
  if (!specNode) continue;

  // この設定ノードがどの ProviderNode の入力 Handle にも接続されていないか確認
  const isConnectedToProvider = edges.some(
    e => e.source === specNode.id && nodes.some(
      n => n.id === e.target && (n.data as WorkflowNodeData).type === 'provider'
    )
  );

  if (!isConnectedToProvider) {
    warnings.push({
      type: 'disconnected_optional',
      nodeId: specNode.id,
      message: `${nodeType} が ProviderNode に未接続。グラフ全体から自動採用されています (将来この自動採用は削除予定)`,
    });
  }
}
```

### Step 8: ambiguous_provider warning を追加

```typescript
// 複数 ProviderNode が存在し CONFIG_INPUT 未接続の GenerateNode がある場合
const providerNodes = nodes.filter(n => (n.data as WorkflowNodeData).type === 'provider');
if (providerNodes.length > 1) {
  for (const genNode of nodes.filter(n => (n.data as WorkflowNodeData).type === 'generate')) {
    const isConnected = edges.some(
      e => e.target === genNode.id && e.targetHandle === HANDLE_IDS.CONFIG_INPUT
    );
    if (!isConnected) {
      warnings.push({
        type: 'ambiguous_provider',
        nodeId: genNode.id,
        message: 'グラフに複数の ProviderNode が存在し、この GenerateNode は ProviderNode に未接続です。findNode フォールバックで先頭の ProviderNode が採用されますが、意図と異なる可能性があります。',
      });
    }
  }
}
```

### Step 9: return に warnings を追加

```typescript
    return {
      errors,
      warnings,
      isValid: errors.length === 0,
      canGenerate,
    };
```

### Step 10: NodeEditor.tsx を更新して warnings を利用

`NodeEditor.tsx` の L240 を更新:

```typescript
const { errors, warnings } = useWorkflowValidation(nodes, edges);
```

warnings が 1 件以上ある場合に NodeEditor 下部に警告バナーを表示する (アプリのトースト/通知コンポーネントを調査して合わせること)。最小実装として div を使った警告バナーでよい:

```tsx
{warnings.length > 0 && (
  <div className="absolute bottom-4 left-1/2 -translate-x-1/2 z-50 max-w-lg w-full px-4">
    <div className="bg-yellow-900/80 border border-yellow-600 rounded-lg p-3 text-xs text-yellow-200 space-y-1">
      {warnings.map((w, i) => (
        <p key={i}>⚠ {w.message}</p>
      ))}
    </div>
  </div>
)}
```

### Step 11: 型チェック確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

### Step 12: 既存テスト確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx vitest run
```

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

エラーなし。

## 完了条件 (Acceptance Criteria)

- AC-6: `warnings` 配列に `disconnected_optional` と `ambiguous_provider` が含まれ、NodeEditor に警告バナーが表示される
- AC-7: `provider_mismatch` エラーがエッジ接続された不整合ノードで検出される
- `ValidationResult` に `warnings: ValidationError[]` が存在すること
- `useWorkflowValidation.ts` の return が `{ errors, warnings, isValid, canGenerate }` であること
- `multiple_provider_connection` の error チェックが実装されていること
- `npx tsc --noEmit` エラーなし

## コミットメッセージ案

```
feat(node-editor): extend validation with warnings for disconnected_optional and ambiguous_provider
```
