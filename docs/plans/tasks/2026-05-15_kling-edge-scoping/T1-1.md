# T1-1: HANDLE_IDS 拡張 + ValidationError 型拡張

依存タスク: なし (起点タスク)

## 目的

`lib/types/node-editor.ts` の `HANDLE_IDS` 定数に ProviderNode の 6 個の入力 Handle ID を追加し、型定義 (`ValidationError`) に新しいエラー/警告タイプを追加する。後続タスク全体が参照する定数・型の基盤を確立する。

## 対象ファイル

編集:
- `/Users/noritakasawada/AI_P/practice/movie-project/movie-maker/lib/types/node-editor.ts`
  - L528-576: `HANDLE_IDS` 定数 (6 行追加)
  - L327-331: `ValidationError.type` union 型 (3 種追加)

## 実装手順

### Step 1: `HANDLE_IDS` に 6 個の Input 定数を追加

`HANDLE_IDS` の `as const` ブロック末尾 (`STITCH_VIDEO_OUTPUT` の直後、`} as const;` の前) に挿入する:

```typescript
  // ProviderNode 入力 (Kling/Runway/Hailuo 設定エッジスコープ)
  KLING_MODE_INPUT: 'kling_mode_input',
  KLING_ELEMENTS_INPUT: 'kling_elements_input',
  KLING_END_FRAME_INPUT: 'kling_end_frame_input',
  KLING_CAMERA_CONTROL_INPUT: 'kling_camera_control_input',
  ACT_TWO_INPUT: 'act_two_input',
  HAILUO_END_FRAME_INPUT: 'hailuo_end_frame_input',
```

挿入後の末尾 (L572-576 相当) は以下の形になること:

```typescript
  STITCH_VIDEO_OUTPUT: 'stitch_video_output',
  // ProviderNode 入力 (Kling/Runway/Hailuo 設定エッジスコープ)
  KLING_MODE_INPUT: 'kling_mode_input',
  KLING_ELEMENTS_INPUT: 'kling_elements_input',
  KLING_END_FRAME_INPUT: 'kling_end_frame_input',
  KLING_CAMERA_CONTROL_INPUT: 'kling_camera_control_input',
  ACT_TWO_INPUT: 'act_two_input',
  HAILUO_END_FRAME_INPUT: 'hailuo_end_frame_input',
} as const;
```

### Step 2: `ValidationError.type` に新しいタイプを追加

`L327-331` の `ValidationError` インターフェースを以下のように更新する:

変更前:
```typescript
export interface ValidationError {
  type: 'missing_node' | 'disconnected' | 'invalid_value' | 'provider_mismatch';
  nodeId?: string;
  message: string;
}
```

変更後:
```typescript
export interface ValidationError {
  type:
    | 'missing_node'
    | 'disconnected'
    | 'invalid_value'
    | 'provider_mismatch'
    | 'disconnected_optional'   // 設定ノードが ProviderNode に未接続 (新 Phase 1: warning)
    | 'ambiguous_provider'      // 複数 ProviderNode + CONFIG_INPUT 未接続 GenerateNode
    | 'multiple_provider_connection'; // CONFIG_INPUT に複数 Provider 接続
  nodeId?: string;
  message: string;
}
```

### Step 3: 型チェック確認

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

エラーなしを確認する。

## 検証

```bash
cd /Users/noritakasawada/AI_P/practice/movie-project/movie-maker && npx tsc --noEmit
```

エラー 0 件であること。

## 完了条件 (Acceptance Criteria)

- Design Doc §5-2 で定義された 6 個の Handle ID (`KLING_MODE_INPUT`, `KLING_ELEMENTS_INPUT`, `KLING_END_FRAME_INPUT`, `KLING_CAMERA_CONTROL_INPUT`, `ACT_TWO_INPUT`, `HAILUO_END_FRAME_INPUT`) が `HANDLE_IDS` に追加されている
- 各 ID の値がスネークケース `_input` サフィックス形式 (`'kling_mode_input'` 等) であること
- `ValidationError.type` に `'disconnected_optional'`, `'ambiguous_provider'`, `'multiple_provider_connection'` の 3 種が追加されている
- `npx tsc --noEmit` がエラーなしで通過すること
- 既存コードへの破壊的変更がないこと (型の union 拡張のみ)

## コミットメッセージ案

```
chore(types): add 6 ProviderNode input Handle IDs and extend ValidationError types
```
